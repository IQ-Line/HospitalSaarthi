- Could you explain citus again, as better-auth tables not having iq-tenant-id suggests to me they're not part of the citus split thing. I'm assuming only one row will have the unique id (that auth_user_id links to)

- I may have to explain kind being user, agent, service in the users table, i'm particularly confused about the 'agent' one

- I was curious where we're storing these strings: "opd:registration:search:advanced" as in the erd.json diagram I couldn't catch it but perhaps I may have missed it

- Regarding the (cdc) audit log, we may implement a downstream consumer audit log service. Accordingly my EM may flag this in our design as user-management also having this table needlessly. I wanted to understand then if we should or shouldn't include it, arguments for and against, then we'll make a call

- I wanted to confirm if department/sub-department thing is handled here, I'm guessing the user_department_assignments.is_primary and department_projection.parent_department_id handles the same