# HIMS Cleanup & Refinement — Master Map (v1)

> **Branch:** `dev--improved-v1` (off `dev`) · **Produced:** 2026-06-21 · **Status:** analysis complete, execution not started
> **How produced:** 13-area parallel deep-dive audit + completeness critic + sequencing synthesis (15 agents), each comparing *current code* vs *intended design (ADRs/HLDs/LLDs + GitHub issues)* vs *ecosystem-canonical approach*. Key claims spot-verified inline.
> **This is the durable index for the cleanup initiative. Update §11 (Session log) and the Decision register as work proceeds.**

---

## 0. How to use this doc

This is the single source of truth for the "clean up & set things up properly" initiative running on the side of mainline dev work. It is **not** a re-architecture — intended architecture is preserved; we are closing the gap between intent and implementation, removing hacks, and standardizing.

- §4 **Concern-area register** — the 20 areas, health, core problem, top gaps.
- §5 **Decision register** — every decision only the user can make. *Resolve these before/when executing the relevant area.* This is the most reusable section across sessions.
- §6 **Roadmap** — dependency-ordered phases (what blocks what).
- §7 **Conflicts & overlaps** — where areas collide; decide once, not N times.
- §11 **Session log** — append what we did each session.

---

## 1. Ground rules (invariants for this initiative)

1. **Pre-production ⇒ DB state is disposable.** Migrations, seed data, and DB can be rebuilt from scratch. Prefer correct end-state over incremental band-aids. *(Assumption A1 — confirm no long-lived shared dev DB needs in-place migration.)*
2. **Do not alter intended architecture.** Modules ≠ services (a module need not be its own service); modular-monorepo boundaries hold; polyglot is fine.
3. **Verify ecosystem-canonical approach before judging a mechanism** (Context7/docs). Don't "make the existing approach work" if it's the wrong approach; check if a sibling (esp. the Python side) already does it the standard way.
4. **Read the standing principles**: `CLAUDE.md`, `docs/architecture/lld/repo-structure/01-monorepo-setup.md`, `docs/architecture/analysis/03-database-principles.md`, the ADRs.
5. **No `tsc`/`tsc -b` locally** (WSL2 freeze) — typecheck belongs in CI.

---

## 2. Verified facts (confirmed this session)

| Fact | Evidence |
|---|---|
| CI runs on `main` **only**, never on the `dev` trunk; integration + typecheck stages commented out. No GitLab/Azure pipeline exists. | `.github/workflows/ci.yml:4-7`; `gitlab-registry-secret` is only a k8s imagePullSecret |
| PR #65 (per-module hand-written SQL migration pattern) is **MERGED into dev**; issue #66 (reverse → drizzle-kit) is **OPEN**. | `gh pr view 65` (merged 2026-05-19), `gh issue view 66` |
| **62** local branches already merged into `dev`. Active worktrees exist **inside** `.claude/worktrees/` (7) **and outside the repo** (`../HIMS-pr46-merge2`, `../The-HIMS-pr140`, `/tmp/pr267-review` prunable). | `git branch --merged dev`, `git worktree list` |
| Zero `__tests__/` dirs; 282 sibling TS test files. | `find … -name '*.test.ts'` |
| Only empi/record-foundation/integration-hub have `drizzle.config.ts` + `drizzle-kit`; all TS modules run `apply-migration.ts` with hardcoded file arrays, no tracking table. | `modules/*/src/schema/apply-migration.ts` |
| smart-report-v2 has **zero references** in-repo and 404s for the current `gh` token. | grep + `gh repo view IQ-Line/smart-report-v2` |

---

## 3. Open assumptions still to verify (load-bearing)

- **A1 — DB truly disposable.** Multiple destructive recommendations (fresh drizzle baselines, schema rebuild, branch deletion) depend on it. Confirm no shared long-lived dev DB.
- **A2 — smart-report-v2 nature.** Language? service vs library? published where? Premise ("monorepo meant to consume it") is unverifiable from this environment — needs user/org answer + repo access.
- **A3 — per-pod isolated event bus** is literally true in every env (drives the "dead cross-process subscription" conclusions). Inferred from topology, not runtime-observed.
- **A4 — Citus** is never actually applied in dev (env-gated probe unset) → cluster is plain single-node PG; "Citus-ready" shape unproven until enabled.

---

## 4. Concern-area register (20 areas)

Health: **good** / **fair** / **poor** / **broken**. 13 audited + 7 found by the completeness critic.

| # | Area | Health | Core problem (one line) |
|---|------|--------|--------------------------|
| A | migrations | poor | Hand-written SQL re-run on every boot, no tracking table/journal; drizzle-kit set up in name only; should be drizzle-kit for all TS modules (issue #66), matching master-data's Alembic. |
| B | tests | poor | No single convention (3 conflicting docs + user pref); unit/integration split unimplemented (`.integration.test.ts` polluted by non-DB tests); PR #35 stale; CI doesn't gate dev. |
| C | nx | fair | Never adopted Nx 22 plugin inference → all targets hand-authored & drifted; services lack lint; only 6/36 typecheck; one broken `@nx/vite:test` target. |
| D | external-repo (smart-report-v2) | poor | Not integrated at all; a *different* external reporting repo (pdf-platform) is already hand-reimplemented twice (TS+Python) with no shared spec; templates line-scraped from a legacy repo. |
| E | authz | poor | Cerbos policies hand-copy a per-action 3-rule template (no derivedRoles/exportVariables); unconditional super-admin bypass contradicts #93; centralization PR stack abandoned; **two authz directions live on dev**. |
| F | authn | poor | Login is **email-primary** — the inverse of ADR-0003's username-primary mandate; better-auth `username()` plugin never installed; BFF Token Handler doesn't exist (bff is a dumb proxy). |
| G | event-bridge | poor | TS SDK now widely adopted, but the **cross-process/cross-language bridge** (the point of #30) was never built; per-pod isolated buses ⇒ dead subscriptions; organic catalog diverged from the designed one. |
| H | module-boundaries | fair | `@nx/enforce-module-boundaries` configured but **toothless** (depConstraints tags don't match real tags); master-data is a module tagged `type:service` w/ embedded FastAPI; dead abdm-adapter scaffolding. |
| I | database | fair | OPD uses `tenant_id` not `iq_tenant_id`; Citus distribution never applied/verified; catalog tables not declared as reference tables; OPD reads `registration.*` cross-schema; stale per-module-DB infra contradicts ADR-0013. |
| J | hygiene | fair | 62 merged branches + worktrees (780M); 32 committed source-maps; stray workspace files; `.gitignore` gaps (.opencode 103M, scratch, tmp); ADR-0031 numbering collision. |
| K | spec-first | poor | Spec-first is aspirational: only 1/11 modules has any spec↔runtime check; no generated clients exist; Python is code-first w/ unautomated YAML exports; demonstrable route drift. |
| L | frontend | good | Healthiest area; problems are drift not design: LLD documents an abandoned permission model; `@cerbos/react` wired but 0 uses; #100 dup tenant-header; untracked mock layers (#112 anti-pattern). |
| M | issues-triage | poor | Tracker drifted from code: realized issues still open (#11/#33/#34/#129/#143); abandoned authz stack never closed; body-less conflicting feature PR wave. |
| N | **ci-cd-delivery** *(critic)* | poor | **No delivery half of CI/CD**: k8s deploys 13 services on mutable `:dev-latest`, but nothing builds/pushes images. Green CI proves nothing about what ships. |
| O | **dependency-drift** *(critic)* | poor | TS declared `^5`/`~5.9`/`>=5.6`/`^6` across packages but resolves to TS 6.0.3; Fastify declared 5 ways; no `engines.node` anywhere; no pnpm `catalog:`/single-version policy. |
| P | **observability** *(critic)* | poor | No tracing/metrics/structured-logging convention; raw `console.*` in source; no correlation IDs; no `/metrics`; the synchronous cross-service HTTP that runs everything is untraced. |
| Q | **dev-seeding/bootstrap** *(critic)* | fair | 5+ overlapping seed/bootstrap subsystems, no single owner; web reaches 4 levels into `packages/dev-bootstrap/src`. |
| R | **error-handling** *(critic)* | fair | No shared error package / RFC7807; per-module ad-hoc errors; most services never call `setErrorHandler` ⇒ inconsistent error envelopes (contract gap for a spec-first platform). |
| S | **docker-infra** *(critic)* | fair | Inconsistent image build (one-off Dockerfiles); cerbos pinned to `:latest` (non-reproducible policy-compile gate); pgbouncer on a `bitnamilegacy` image. |
| T | **i18n/a11y** *(critic)* | poor (low pri) | No i18n foundation in an India/AIIMS-scope SPA; retrofitting later is costly. Possibly out of Phase-1 scope. |

### Per-area detail (top gaps with severity/effort)

**A — migrations:** `[crit/L]` no tracking table → every `.sql` re-runs on boot · `[crit/M]` ghost migration files silently never applied (UM `0003_*`, registration `0004_*`, configurator `009_*`) · `[high/M]` duplicate/ambiguous ordering (two `0001_*` in UM; two `005_*`/`009_*` in configurator) · `[high/M]` `tables.ts` source-of-truth vs applied SQL can drift, no gate · `[high/XL]` PR #65 generalized the wrong pattern. Tracked: **#66**, PR #65 (merged), per-module `db:migrate`+`db-migrate` dup targets. master-data Alembic is the in-house precedent.

**B — tests:** `[crit/S]` CI triggers on `main` only → dev PRs never tested · `[high/M]` unit/integration split unimplemented; `.integration.test.ts` polluted by 6 non-DB tests · `[high/L]` PR #35 uses per-module duplicated configs vs vitest canonical `projects` field · `[high/L]` Python master-data+opd still on in-memory SQLite (incl. files named `*_crud_integration.py`) · `[med/S]` `ci:pr` runs forbidden `tsc`. Tracked: **#36, PR #35, #137**.

**C — nx:** `[crit/S]` CI never runs on trunk · `[high/L]` no plugin inference (root cause of drift) · `[high/S]` `@nx/vite:test` used but plugin not installed · `[high/M]` 10 services lack lint; 6/36 typecheck · `[med/S]` dup migrate targets in 11 projects · `[low/S]` no `.nxignore`. Note: issue #113 item 5 (billing-svc:serve "malformed") is **wrong** — it's valid command-sugar. Tracked: **#113**.

**D — external-repo:** `[high/S]` smart-report-v2 absent + unverifiable (A2) · `[high/M]` pdf-platform HTTP contract hand-reimplemented twice (TS `packages/pdf-client` + Python `modules/opd/.../pdf_platform_client.py`) · `[high/L]` report templates from a non-reproducible line-scrape (`tools/extract-opd-report-templates.mjs`) · `[med/S]` no ADR records the external-reporting decision.

**E — authz:** `[crit/M]` unconditional super-admin bypass (contradicts #93) · `[high/L]` policies don't use derivedRoles/exportVariables · `[high/M]` policies reference capability keys the master-data catalog never generates · `[high/L]` empi/configurator/master-data/opd have no real PEP on dev. Tracked: abandoned stack **#135→#136→#139→#141→#149**; **#89/#93/#94**, #125/#128, #60, ADR-0004/0032. dev still runs the OLD `resolveTarget` pattern (`packages/ts-sdk-authz/src/plugin.ts:90,186`).

**F — authn:** `[crit/M]` login email-primary; `username()` plugin never installed (violates ADR-0003) · `[high/M]` no synthetic/sub-addressed email anchor; `createUser` requires a real RFC email · `[high/L]` BFF Token Handler missing (bff is `@fastify/http-proxy` only) · `[med/S]` dev-auth doc documents the wrong (email) reality. Tracked: **#114, #90**; `user-management-authn-solution-deliberation.txt` (795-line design record). ADR-0003/0015.

**G — event-bridge:** `[high/L]` cross-process bridge (core of #30) doesn't exist; no `/internal/events` receiver · `[high/L]` py-sdk-events (PR #31) unmerged; master-data/opd publish nothing · `[high/M]` none of #30's 10 catalog events exist; organic catalog diverged & undocumented · `[med/S]` dead subscription UM←`configurator.tenant_module.*` (cross-process, unreachable). Tracked: **#30, PR #31**, ADR-0009/0017.

**H — module-boundaries:** `[crit/M]` enforce-module-boundaries depConstraints reference tags no project carries → unenforced · `[high/M]` inconsistent/missing Nx tags · `[high/L]` master-data tagged `type:service` w/ embedded FastAPI, no `services/master-data-svc` · `[med/S]` abdm-adapter + abdm-adapter-svc empty dead scaffolding · `[med/L]` cross-module sync via hand-rolled HTTP clients vs prescribed generated openapi-clients. ADR-0006/0008.

**I — database:** `[high/M]` OPD `tenant_id` vs `iq_tenant_id` · `[high/L]` Citus never applied/verified (A4) · `[high/L]` catalog tables FK'd by distributed tables not declared reference tables · `[high/L]` OPD cross-schema reads into `registration.*` · `[med/S]` stale `infra/db/create-module-databases.sql` contradicts ADR-0013. Tracked: **#91, #92**, #25/#26, ADR-0012/0013/0020/0021.

**J — hygiene:** `[high/M]` 7 worktrees in `.claude/worktrees/` (780M) pinning merged branches · `[med/S]` 61 merged branches · `[med/S]` 32 committed source-maps under `packages/ts-sdk-fhir/src/` · `[med/S]` `.gitignore`/`.dockerignore` miss `.opencode/`(103M)/`scratch`/`tmp`/`.worktrees` · `[med/S]` ADR-0031 collision (two files share the number) · `[low/S]` root deliberation `.txt` + `.npmrc` need a home.

**K — spec-first:** `[high/L]` no generated cross-module client mechanism (`packages/openapi-clients` doesn't exist) · `[high/M]` spec↔runtime check on 1/11 modules · `[high/M]` undocumented runtime routes in empi/registration · `[high/L]` Python code-first but YAML is an unautomated export. ADR-0016.

**L — frontend:** `[high/M]` LLD documents abandoned nested PermissionMap (code uses flat capability keys) · `[high/M]` #100 dup tenant-header still live · `[med/M]` `@cerbos/react` wired, 0 hook uses · `[med/M]` untracked create-rx/opd-patients mock layers (#112 anti-pattern) · `[med/L]` 3 parallel route-gating subsystems. Tracked: **#100, #112, #114, #90, #20**. ADR-0018.

**M — issues-triage:** `[crit/L]` authz stack abandoned, never closed; dev runs the pattern it was to delete · `[high/S]` realized issues still open · `[high/M]` testing #35 + py-sdk-events #31 are the canonical foundations others depend on, both stale · `[high/L]` body-less conflicting feature-PR wave (#211/#250/#253/#291/#292). 30 open issues, 15 open PRs.

**N–T — critic-found:** see §4 table. **N (ci-cd-delivery)** and the CI-on-dev gap should be unified into one CI/CD workstream. **O (dependency-drift)** → pnpm `catalog:` single-version policy + `engines.node`. **P (observability)** → shared logging/trace SDK + correlation IDs. **Q (dev-seeding)** → consolidate to one owner. **R (error-handling)** → shared `ts-sdk-errors` + RFC7807. **S (docker-infra)** → shared base image + pin cerbos. **T (i18n)** → likely deferred; flag now.

---

## 5. Decision register (resolve these — only the user can)

> Status: ☐ open · ☑ decided. Record the decision + date inline when resolved.

**Foundational / cross-cutting (gate multiple areas):**
- ☑ **D1 (test layout) — DECIDED 2026-06-21: separated `test/unit` + `test/integration`** (the LLD's documented layout, `01-monorepo-setup.md:168`; also mirrors the Python `tests/` dirs). Implies: move all 282 sibling TS test files into per-project `test/{unit,integration}`; rewrite vitest `include`/`exclude` globs AND `nx.json` `production` namedInput from suffix-based (`*.test.ts`) to **path-based** (`test/**`) in lockstep; correct issue #36 (says siblings) and the earlier `__tests__/` preference. Unit/integration split stays driven by directory + `DATABASE_URL`. Gates: B, C.
- ☑ **D2 (migration direction) — DECIDED 2026-06-21: reverse PR #65, adopt drizzle-kit (#66)** for all TS modules via shared `packages/ts-sdk-db`; regenerate **fresh** baselines from `src/schema/tables.ts` (A1 accepted — DB disposable, no `_journal.json` history stitching). Gates: A, I.
- ☑ **D3 (cross-module sync mechanism) — DECIDED 2026-06-22: ports + hand-written adapters.** No generated `openapi-clients` layer; cross-module sync calls go through a port interface (`ports.ts`) with a hand-written HTTP adapter per consumer (the shape `integration-hub`'s `ConfiguratorHttpIntegrationProfileRepo` already uses). **Amend ADR-0016** to bless this (it currently implies generated clients). Rationale: generated clients add a build-time codegen step + a package for a handful of call sites; hand adapters are the simpler thing that fully does the job and keep the port/adapter paradigm consistent. Gates: H, K, G, D.
- ☑ **D4 (distribution-key name) — DECIDED 2026-06-21: `iq_tenant_id` is canonical.** OPD (`tenant_id`) gets renamed; `database-principles.md §2` (which says `tenant_id`) gets corrected to match. Gates: I, B (fixtures).
- ☑ **D5 (master-data classification) — DECIDED 2026-06-22: normalize to module + `services/master-data-svc`** (matches opd's clean split: `modules/opd` = `type:module` library, `services/opd-svc` = `type:service` app). master-data is currently mis-shaped — in `modules/` but tagged `type:service` + `projectType: application`, serving its own `app.main:app`. **EXECUTION DEFERRED (gated)** to pair with the ruff phase (both touch master-data's Python deeply) and to take the user's call on the Python package boundary (`app/` → `src/master_data/` for the module + a thin `master_data_svc` wrapper). It is a restructure of a *working* service and is NOT a blocker for the per-module vet (master-data is vetted on its current shape). Gates: C, H, I, B, ruff-phase.
- ◐ **D6 (Nx inference) — GOAL DECIDED (drift-free, consistent targets); MECHANISM REFINED 2026-06-22 (surface to user).** Original rec was full plugin inference (`@nx/js`/`@nx/eslint`/`@nx/vite`). **Recon finding that changes the mechanism:** *zero* of the 25 TS projects have per-project `vite.config.ts`/`vitest.config.*`/`eslint.config.*` — config is centralized at the repo root and tests run via explicit `nx:run-commands "vitest run"`. `@nx/vite/plugin` keys off per-project `vite.config.ts`, so it would infer **nothing** unless we scaffold 25+ vite configs; `@nx/js/typescript` build-inference wants a per-project tsconfig.lib/spec/references layout this repo doesn't use. Adopting inference therefore means **adding 25+ config files to fight the repo's clean centralization** — complexity unjustified by present need, and the wrong mechanism for this layout (per the "verify the ecosystem-canonical approach before the mechanism" rule). It is also NOT a blocker for the vet (lint/test run via existing targets; typecheck runs via `tsc` directly). **Refined plan:** keep centralized config; achieve the goal by (a) defining target shapes once in `nx.json` `targetDefaults` (already present) and (b) fixing the concrete target drift (4 SDK phantom `test` targets) — folded into the SDK vet where each package's tests are run anyway. Full inference left as an explicit user decision (it's a standardization change that wants review). Gates: C, H.
- ☑ **D7 (CI trunk) — DECIDED 2026-06-21: yes, retarget CI to gate `dev`** (implied by selecting Phase 0). CI image build/push/deploy (area N) folded into Phase 0/2. Gates: everything observable.
- ☑ **D8 (events vs HTTP as default) — DECIDED 2026-06-22: HTTP-first for Phase 1** (confirmed + agreed; aligns with the 2026-05-18 orchestration decision, HLDs `integration-platform/04`+`05`). Synchronous cross-module calls go via ports+adapters (D3); the event-bridge facade (`/internal/events`) is the **Phase 5** slice and the documented durable target. Events stay intra-process (InProcessEventBus, ADR-0017) until then. Gates: G.
- ☑ **D9 (Citus in dev) — DECIDED 2026-06-21: distribute unconditionally** (dev + CI already run `citusdata/citus:12.1`). Distribution/reference-table creation moves into journaled `--custom` migrations that always run; the old unset-env-gated `create_distributed_table` probes are removed. **Verified on empi** against real Citus. Gates: I (closes risk A4).

**Per-area decisions:**
- ☑ **D10 (super-admin model) — DECIDED 2026-06-22: granular cross-tenant capability bundle** (#93) — "as long as you're sure." No unconditional super-admin bypass; cross-tenant power is an explicit capability bundle subject to the same PDP. Affects E, F, L (Phase 4 authz).
- ☐ **D11 (username uniqueness)** — Global tenant-prefixed handle vs custom `databaseHook` enforcing `(username, iq_tenant_id)`. (Decide before wiring plugin to avoid a second migration.)
- ☐ **D12 (email anchor policy)** — Deliverable sub-addressed (`admin+N@base`, keeps email recovery) vs non-deliverable `{username}@auth.internal` (admin-mediated recovery). Drives recovery flows.
- ☑ **D13 (BFF edge-auth) — DECIDED 2026-06-22: BFF IN SCOPE for edge-auth** (Step 4 of the monorepo-ready campaign). **Confirmed by recon:** `services/bff/src/main.ts` is today a pure `@fastify/http-proxy` — it forwards `Authorization` but does **NO** JWT validation and **NO** ban/revocation check, so a banned user is not cut off until their token expires. Add per-request JWT validation (JWKS) + a cached ban/revocation check → 401 immediately on banned users (`auth.user.banned` already exists in better-auth). Closes the immediate-cutoff gap (#90/#114 direction). Spans F (backend) + L (frontend).
- ☑ **D14 (Python spec strategy) — DECIDED 2026-06-22: code-first** (FastAPI `app.openapi()` exported to `specs/openapi/<module>.v1.yaml` + a CI diff gate that fails on drift). **Amend ADR-0016** for polyglot reality (its "spec-first YAML before handlers" rule holds for TS/Fastify; Python is code-first with an exported, gated spec). Gates: K (master-data, opd specs).
- ◐ **D15 (smart-report-v2) — IDENTIFIED 2026-06-22; integration PARKED to Phase 5.** Confirmed via the IQ-Line gh account (`github.com/IQ-Line/smart-report-v2`): it's a **private TS pnpm/turbo report-rendering service** that supersedes the hand-reimplemented `pdf-platform` client + scraped templates. Not core cleanup; integration mechanism (submodule / pnpm catalog / published package) to be decided in Phase 5 (G/D, external reporting). Gates: G, D.
- ☐ **D16 (frontend authz docs)** — Update LLD to capability-key model vs designate `capability-key-first.md` as single source. And adopt-or-remove `@cerbos/react`.
- ☑ **D17 (schema rename + reference tables) — DECIDED+DONE 2026-06-21:** folded #91 (global_master→master_global, tenant_master→master_tenant) + #92 (Citus reference tables) into Phase 1. Implemented via constant swap + 373 literal replacements across 41 migrations + a guarded idempotent `ALTER SCHEMA` migration 044; verified on real Citus (commit `41926d6f`). #92 global reference tables confirmed in `pg_dist_partition`. *(Caveat: `master_tenant` platform tables are still made reference tables despite carrying `iq_tenant_id` — should be distributed once their PK is reshaped; tracked as a follow-up.)*
- ☑ **D18 (hygiene specifics) — DECIDED 2026-06-22 (confirmed + agreed); branch/worktree deletion DEFERRED to do with user.** `.npmrc` committed; authn deliberation promoted to `docs/`; ADR-0031 collision resolved (abdm-m3→0033) — all done in Phase 0. **Branch + worktree deletion is DEFERRED (not done autonomously):** it is destructive/hard-to-reverse, has near-zero readiness value, and the WIP in the two remaining worktrees has ambiguous provenance — `feat/abdm-adapter-scaffold` holds only stale untracked dupes of the orchestration HLDs (already committed on `dev--improved-v1` @ `d0929d0e`, so nothing at risk); `pr84-review`/`billing_reg_integration` holds 2 uncommitted modified files (`tariff-client.ts`, `auth-session.ts`, branch behind origin by 2) of unclear authorship. Both branches are pushed to origin (recoverable). Left intact pending the user's go-ahead. Gates: J.
- ☐ **D19 (dependency policy)** — Adopt pnpm `catalog:` single-version + `engines.node` across all 42 package.json. Reconcile CLAUDE.md ("ESLint 10"/TS) with installed versions.
- ☑ **D20 (i18n) — DECIDED 2026-06-22: deferred ("let i18n be").** Out of scope for the cleanup + monorepo-ready campaign; revisit as its own slice (area T) post-readiness. Gate: not a blocker for any current phase.
- ☐ **D21 (branch/integration strategy)** — IPD stack (#211 umbrella + #250/#253 targeting `module/ipd-lite`); #169 offline-approach lives in another repo — track here or close?
- ☑ **D22 (lint gating policy) — RATIFIED 2026-06-22, with a cognitive-complexity hard bound.** User ratified the demote-to-warn policy below, **plus**: cognitive-complexity gets a *meaningful upper bound beyond which it MUST be cleaned* → after the **23 functions currently ≥15** (values 42,35,35,33,31,30,30,28,28,25,23,22,22,21,20,20,20,19,19,19,19,18,17) are refactored by real decomposition (not metric-gaming), flip `sonarjs/cognitive-complexity` from `warn` to **`['error', 15]`** (15 = the SonarSource industry-standard default; my earlier 25 suggestion was a churn-minimizing compromise, not principled — corrected). Also add **`max-lines: ['warn', 500]`** so over-long files surface a PR warning. This refactor + flip happens in the per-module deep vet (Step 2). `sonarjs.configs.recommended` was adopted wholesale in `packages/eslint-config/index.js` and never tuned, so ~140 sonarjs rules gated the build. Intentional policy set: **KEEP as error + fix the code** — all *security* (slow-regex, sql-queries, no-clear-text-protocols, hashing) and *cleanliness* (unused-import, redundant-type-aliases, void-use, use-type-alias, no-unused-vars, prefer-single-boolean-return, …) rules; **DEMOTE to warn** (visible, non-gating) — `todo-tag` (TODOs are deliberate signposts in mid-build code, e.g. the ABHA SDK), `cognitive-complexity` + `no-nested-conditional` (forcing them to zero invites helper-function indirection purely to satisfy a metric — the inverse of the simplicity doctrine). Fully reversible (3 lines); the warnings remain as a worklist if a future call prefers fixing them instead. Confirmed false positives (FHIR system URIs, RFC-4122 UUIDv5 SHA-1, trailing-slash regexes, trusted test DDL) get per-line justified `eslint-disable-next-line … -- <reason>`, never blanket disables. Gates: C (nx lint), the CI lint gate (Stage 1).

---

## 6. Sequenced roadmap (dependency-ordered)

> Recommended order from the sequencing synthesis. "blockedBy" = must come after.

- **Phase 0 — Unblock & decontaminate** · areas: **J (hygiene), C (nx quick-fixes only)**. Highest leverage: **retarget CI to `dev` (D7)** flips the repo from "no dev PR tested" to verifiable; remove broken `@nx/vite:test` target, dup migrate targets, dead Makefile targets; delete 61 merged branches + worktrees (confirm first); drop committed source-maps/workspace files; close `.gitignore` gaps; resolve ADR-0031; delete stale `create-module-databases.sql` + empty abdm-adapter scaffolding. *(Nx plugin-inference rebuild deferred to Phase 2.)*
- **Phase 1 — DB & migration foundation rebuild** · areas: **A, I** · blockedBy: J, C. One atomic destructive pre-prod operation: reverse #65 → drizzle-kit (D2) shared via `packages/ts-sdk-db`; rename OPD `tenant_id`→`iq_tenant_id` (D4); reference tables (#92) + distribute `tenant_master.*`; schema rename (#91, D17); enable-or-document Citus (D9); delete stale per-module-DB infra; fix OPD cross-schema reads.
- **Phase 2 — Boundaries, spec-first, Nx inference** · areas: **H, K, C** · blockedBy: A, I. Make enforce-module-boundaries bite (tags); adopt Nx inference (D6); normalize master-data (D5); decide cross-module client mechanism (D3); Python code-first (D14); add lint/typecheck everywhere + a module generator.
- **Phase 3 — Identity (authn) & test infra** · areas: **F, B** · blockedBy: A, I, C, H, K, J. Username-primary rebuild (D10–D13); resolve test layout (D1) + vitest `projects` + uncomment integration CI + Python off SQLite.
- **Phase 4 — Authorization rebuild** · area: **E** · blockedBy: F, A, I, K, H. Close abandoned stack; pick canonical direction; Cerbos derivedRoles/exportVariables + base tenant-isolation policy; remove super-admin bypass (D10); capability keys authoritative from master-data seed; real PEPs everywhere.
- **Phase 5 — Event bridge & external reporting** · areas: **G, D** · blockedBy: H, K, F. Decide D8; merge py-sdk-events; reconcile catalog; smart-report-v2 (D15) once A2 answered.
- **Phase 6 — Frontend reconciliation & triage closeout** · areas: **L, M** · blockedBy: E, F, K. Doc reconciliation (D16); #100/#112/#114/#90; close realized issues + abandoned stack; gates on deferred items.
- **Continuous / fold-in:** **N (ci-cd-delivery)** into Phase 0/2 CI work; **O (deps)** Phase 0/2; **P (observability)**, **R (error-handling)**, **S (docker-infra)**, **Q (seeding)** as their own slices when the dependent foundation is ready; **T (i18n)** per D20.

---

## 7. Cross-area conflicts & overlaps (decide once)

- **CI scope** triple-claimed (C, B, M) + the uncovered delivery half (N) → **one** CI/CD workstream.
- **drizzle-kit (#66)** owned by A, M, C → single owner (A).
- **Generated cross-module clients** claimed by K, H, and touched by G, D → **D3 decides for all four**.
- **Citus reference tables / schema rename (#91/#92)** in I and M → one migration rebuild (Phase 1).
- **Nx tags / enforce-module-boundaries** in H and C → single repo-wide edit (Phase 2).
- **master-data module-vs-service** in C, H, I → **D5 resolves all**.
- **Authz stack abandonment + super-admin** in E and M → one authz-direction decision (D10/Phase 4).
- **Auth-flow (#90/#114, BFF)** spans F (backend) and L (frontend) → don't split.
- **Capability-key namespace** spans E (policies), L (gates), and master-data seed → one naming decision.
- **Stale per-module-DB infra** in I, J, and k8s secrets → coordinate deletion with the shared-DB story.

---

## 8. Quick wins (low effort, high value — mostly Phase 0)

CI→dev (D7) · remove broken `@nx/vite:test` + dup migrate targets + dead Makefile targets · move `tsc` to CI-only · delete 61 merged branches + worktrees (confirm) · drop 32 source-maps + stray workspace files + close `.gitignore` gaps · delete `create-module-databases.sql` + empty abdm-adapter scaffolding · fix ADR-0031 collision · close realized issues (#11/#33/#34/#129/#143) + abandoned authz stack · drop wrong issue #113 item 5.

---

## 9. Biggest risks

1. **Two live authz directions** (old `resolveTarget` on dev vs abandoned stack vs out-of-band #289) + unconditional super-admin bypass = live cross-tenant hole until one is canonical.
2. **Citus never exercised** (A4) → distribution/reference-table failures surface in prod, not CI.
3. **Migration rebuild must be one atomic op** with the DB reshape, or the destructive-rebuild tax is paid twice; every new `.sql` doubles conversion cost.
4. **Spec drift unenforced** → shipping the body-less feature-PR wave before Phase 2 locks in N more drifting modules.
5. **smart-report-v2 unverifiable** (A2) → risk of two parallel external-reporting integrations.
6. **Events silently single-process** while docs claim cross-service async → correctness landmine for new consumers.
7. **authn is the inverse of its ADR** → building authz/frontend on the wrong identity model means redoing both.

---

## 10. Tracked-work disposition (summary)

- **Close as realized-on-dev:** #11, #33, #34, #129, #143 (confirm remaining phases first).
- **Close as abandoned:** authz stack #135/#136/#139/#141/#149 (re-scope a fresh authz cleanup).
- **Revive as foundation:** PR #35 (tests — but switch to vitest `projects`), PR #31 (py-sdk-events).
- **Fold into cleanup phases:** #66→A, #113→C, #91/#92→I, #89/#93/#94→E, #100/#112/#114/#90→L/F, #36/#25/#26→KB docs.
- **Decide branch strategy:** #211/#250/#253 (IPD), #169 (offline, lives in another repo).

---

## 11. Session log

| Date | Session | What happened |
|---|---|---|
| 2026-06-21 | 1 | Deep-dive analysis (15-agent workflow). Created branch `dev--improved-v1`. Wrote this map. Verified CI-on-main, PR#65 merged/#66 open, 62 merged branches. |
| 2026-06-21 | 1 | **First work block chosen:** Phase 0 + Phase 1 + AuthZ + AuthN. **Decisions:** D1=separated `test/unit`+`test/integration`; D2=drizzle-kit + fresh baselines; D7=CI→dev. Internal execution order: Phase 0 → Phase 1 → AuthN → AuthZ (authz depends on authn + capability-key correctness; both pull in the minimal Phase-2 boundary/spec bits they need). |
| 2026-06-21 | 1 | **Phase 0 committed: `286eefe6`** (scoped — no unrelated WIP swept in; `.npmrc` now tracked). **Branch/worktree cleanup:** removed 5 of 7 `.claude/worktrees` (2 kept — uncommitted work: `feat/abdm-adapter-scaffold`, `billing_reg_integration`); pruned stale `/tmp/pr267-review`; **deleted 58 merged branches** (77→19 local). Preserved all branches needed downstream (authz-level-1/2/3a/3b, fix/authz-corrections, py-sdk-events, chore/testing-scaffolding, record-foundation-module, module/ipd-lite). **abdm-adapter dead-dir deletion NOT done** — it's a live prototype (ADR-0030) mid-transition to integration-hub (see DEVNOTE 2026-05-28); reclassified from "quick win" to **deferred**. **Phase 0 complete. Next: Phase 1 (DB + migrations drizzle-kit rebuild) — recommend a fresh focused session.** |
| 2026-06-21 | 1 | **Phase 0 executed (safe items):** CI now triggers on `[dev, main]`; `.gitignore` gaps closed (`.opencode/`,`scratch/`,`tmp/`,`.worktrees/`,`*.code-workspace`); untracked the 32 `ts-sdk-fhir` build artifacts + removed 2 stray `*.code-workspace` files; fixed broken `@nx/vite:test` in `ts-sdk-sequence`+`ts-sdk-db` (→ `vitest run`); ADR-0031 collision resolved (abdm-m3 → **0033**, README indexed + note); promoted authn deliberation → `docs/architecture/auth/username-primary-authn-deliberation.md`. **Deferred to Phase 1** (same edits get rewritten there): dup `db:migrate` targets, `create-module-databases.sql`, Makefile `db-migrate`. **Needs user confirm:** branch(62)+worktree cleanup, abdm-adapter dead-dir deletion, whether to commit. **Not yet committed.** |
| 2026-06-21 | 1 | **Phase 1 STARTED — empi reference proven end-to-end against real Citus 12.1.** Built shared `applyMigrations`+`createPool` in `ts-sdk-db`; converted empi to drizzle-kit (fresh baseline from `tables.ts` + custom pg_trgm + custom citus-distribute); rewired runtime apply path; deduped project.json. **Verified:** fresh apply + idempotent re-run + 7 tables + tracking table `drizzle.__drizzle_migrations_empi` + pg_trgm/gin index + all 7 tables distributed by `iq_tenant_id` + real `db-migrate` command path. See §12 for the canonical recipe. **Next: fan out the other 7 TS modules + DB reshape. empi reference not yet committed.** |
| 2026-06-21 | 1 | **empi reference committed `94753ed3`.** **Phase 1 fan-out COMPLETE — all 7 remaining TS modules converted to drizzle-kit + verified against real Citus** (parallel workflow, 7 agents): billing (4 dist), configurator (1 dist `tenant_modules` / 5 ref incl. `organizations`+`tenants` ✓), registration (2 dist), pharmacy (4 dist), user-management (7 dist / 1 ref `capabilities` / **5 `auth.*` local-exception**; FK-web solved via drop→reference→distribute→re-add + `multi_shard_modify_mode=sequential`), record-foundation (2 dist), integration-hub (9 dist). All applied twice (idempotent), distribution confirmed in `pg_dist_partition`. Scope clean (only the 7 modules touched). **CRITICAL FINDING:** fresh baselines exposed `tables.ts`↔hand-written-SQL drift (missing CHECKs/FKs/partial indexes) → see §12 schema-parity follow-up. **Next: commit fan-out; then schema-parity restore + Python/DB reshape tail.** |
| 2026-06-21 | 1 | **Phase 1 fan-out committed `0f08202e`** (scope-guarded: only the 7 modules + lockfile + map). **TS drizzle-kit conversion COMPLETE for all 8 modules**, each verified against real Citus. Remaining Phase 1: (a) schema-parity restoration [task] — restore drifted CHECKs/FKs/partial indexes into `tables.ts` + add CI empty-diff gate; (b) Python/DB reshape — OPD `tenant_id`→`iq_tenant_id`, #91 schema rename, delete stale `create-module-databases.sql` + k8s `*_DATABASE_URL` secrets, fix OPD cross-schema reads, Makefile `db-migrate` rewrite, `database-principles.md §2` fix, dev `docker-compose` `citus-init.sql` bind-mount fix. Throwaway verify DB `hims-verify` (:5444) left running. **Recommend a fresh focused session for the Python/DB reshape.** |
| 2026-06-21 | 1 | **Phase 1 Python/DB reshape COMPLETE (task 11) — committed `41926d6f`.** Workflow: 5 parallel edit slices (exclusive file ownership) + 2 real-Citus verifies; 2 bugs caught by verification and fixed. **OPD** `tenant_id`→`iq_tenant_id` via guarded info-schema DO-block migration `0004` (Postgres cascades PK/FK/index); models keep ORM attr `tenant_id` but physical/key column `iq_tenant_id`; `__table_args__` string refs (Index/FK/UniqueConstraint) fixed to the new key (the verify-caught bug — `mapped_column("iq_tenant_id")` changes the *key*). Verified full chain `001..0004` on Citus: 0 `tenant_id`, 21 `iq_tenant_id`, 42 PK/FK intact + cascaded, idempotent. **master-data #91** `global_master`→`master_global` / `tenant_master`→`master_tenant`: constant swap (2 files) + **373 hardcoded literals across 41 migrations** + guarded `ALTER SCHEMA` migration `044` (no-op fresh / rename existing) + SQLite test ATTACH fixtures + configurator cross-schema read + ADRs/LLDs/docs. **#92** reference tables confirmed. Verified alembic `001..044` on Citus (`master_global`/`master_tenant`, ref tables in `pg_dist_partition`, single head). **infra:** `citus-init.sql`→extensions-only (fixes the `CREATE SCHEMA` vs drizzle-baseline conflict) relocated to `infra/db/initdb/` + WSL2 directory-mount; unified `DATABASE_URL` (pydantic `AliasChoices` fallback + `postgresql://`→`postgresql+psycopg://` normalization in `database.py`) + collapsed the 3 per-module `*_DATABASE_URL` k8s secrets; Makefile single-pass `db-migrate` (dropped obsolete double-configurator run); `database-principles.md §2` →`iq_tenant_id`. **Phase 1 fully DONE (TS + Python).** Deferrals→§12 follow-ups. *(Note: the untracked, not-ours `026_user_management_catalog_seed.py` dup-revision file blocks in-place master-data alembic for whoever has it — left untouched; verified via tracked-only temp copy.)* |
| 2026-06-21 | 1 | **Schema-parity restoration COMPLETE (task 10)** — restored the drifted integrity objects INTO `tables.ts` + regenerated, all verified against Citus with the empty-diff gate passing: configurator (4 FKs), pharmacy (15 CHECKs + 8 indexes), record-foundation (2 CHECKs + 1 composite FK), user-management (1 partial index). Handled a drizzle gotcha (qualified col refs rejected in `ALTER ADD CHECK` → bare-column `sql.raw`). **Added a CI drift-gate** (`.github/workflows/ci.yml` "Drizzle migration drift check"): runs `drizzle-kit generate` per module and fails on any diff — `tables.ts` can't silently diverge from migrations again. **Closes the audit's "tables.ts vs SQL drift, no gate" gap. Phase 1 TS side fully done + gated.** Remaining Phase 1 = Python/DB reshape (task 11). |
| 2026-06-22 | 3 | **Monorepo-ready campaign — Step 1 (Enablers).** Synced the Decision Register with the user-ratified decisions: D3 (ports+hand-adapters, amend ADR-0016), D8 (HTTP-first; event bridge=Phase 5), D10 (granular super-admin bundle), D13 (BFF edge-auth IN SCOPE — recon confirmed BFF is a pure proxy with no JWT validation/ban check), D14 (Python code-first spec + CI diff), D15 (smart-report-v2 = private TS report service, parked to Phase 5), D20 (i18n deferred), D22 (ratified + cognitive-complexity → `['error',15]` after refactoring the 23 fns, add `max-lines: ['warn',500]`). **cerbos pin DONE:** 3 OSS `ghcr.io/cerbos/cerbos:latest` refs (ci.yml ×2, docker-compose ×1) → **`0.53.0`** (current stable, tag verified HTTP-200 on GHCR; `v`-prefix 404s); internal Azure `dev-latest` left alone. **Evidence-based REFINEMENTS (surface to user):** (D6) full Nx plugin inference is the *wrong mechanism* here — recon found **zero** TS projects have per-project vite/eslint configs (centralized), so `@nx/vite/plugin` would infer nothing and adopting inference means adding 25+ config files; refined to keep centralized config + fix the 4 SDK phantom test-targets in the SDK vet. (D5) master-data split deferred to pair with the ruff phase + user's package-boundary call (risky restructure of a working svc; not a vet blocker). (D18) branch/worktree deletion deferred — destructive, low value, ambiguous WIP provenance, HLD-preservation concern moot (already committed @ d0929d0e). **Next: Step 2 — per-module deep vet.** dev still `12963b72`. |
| 2026-06-22 | 3 | **Step 2 — per-module DEEP VET + first remediation.** 20-agent workflow (10 deep read-only vets + 10 INDEPENDENT adversarial verifiers) over all 10 modules; durable report `docs/architecture/cleanup/01-module-vet-2026-06-22.md` (raw JSON in gitignored `scratch/`), commit `dbfab065`. Headline: no module overall-red; pharmacy+integration-hub green, rest yellow; **dominant gap = test sincerity** (no real-DB tests outside pharmacy; billing happy-path-only). Adversaries earned their keep — caught the configurator spec mischaracterization + opd cross-tenant READ gap, recategorized doc-hygiene out of "fix-now". **KEYSTONE found:** real-DB test harness already exists (`packages/ts-sdk-testing/src/db-setup.ts`), so test-sincerity is unblocked. **Lesson:** fixes are PER-MODULE-COHERENT, not cross-module-batchable. **First remediation `d8faae9f`:** rebuilt configurator's CORRUPTED (non-parseable) OpenAPI region 648-967 — scrambled ops rotated into each other; reconstructed against the runtime handlers, 10 ops now map 1:1; verified independently (parses, scope-confined, 49 refs resolve, route-match). dev still `12963b72`. |
| 2026-06-22 | 2 | **LINT-GATE GREEN (TS) — areas C+H.** Root-caused the enabled-but-red Stage-1 lint gate: **169 TS errors → 0** across 15 projects. (1) **Tag taxonomy fix (area H):** 5 SDK pkgs were mistagged `type:lib` (no depConstraint references it) → `type:sdk`; tagged `@hims/dev-bootstrap`; this turned 22 `@nx/enforce-module-boundaries` errors into 4 residuals (needs `nx reset` — graph daemon caches tags). Residuals: dev-bootstrap relative→`@hims/dev-bootstrap` scope imports; the module→service `authUser` cycle = justified gated `eslint-disable` (dev-only; clean relocation would leak dev internals into the module's public API — gated follow-up); a TS inline-import-type false positive (hoisted to `import type`); a test self-import→relative. (2) **D22 policy tune:** demoted `todo-tag`/`cognitive-complexity`/`no-nested-conditional` to **warn** (71 errors); kept all security+cleanliness rules as error. (3) **Remaining ~76 fixed via workflow** (15 projects, fix→adversarial-review): mechanical cleanups + per-line justified disables for confirmed false positives (FHIR system URIs, RFC-4122 UUIDv5 SHA-1, trailing-slash/`@`-delimited regexes, trusted test DDL, exported type-alias names). **Adversarial reviewer caught a real regression** (configurator over-engineered the email `slow-regex` into `isValidEmail`, left a dangling `EMAIL_RE`→TS2304; reverted to the rubric's justified-disable, matching create-user.ts) and one lost agent edit (integration-hub fix didn't persist → redone by hand). **Verified:** 15/15 TS lint green; 6/6 typecheck green; zero test-assertion regressions (UM 228, um-svc 22, integration-hub 139, pharmacy 47, configurator 42, empi 36, ts-sdk-identity 21, registration 17, billing 14, ts-sdk-fhir 5, ts-sdk-abha 22, ts-sdk-tenant 3, +). **Out of scope (pre-existing, separate items):** master-data/opd **`ruff` 339 errors** (Python lint never enforced); 4 SDK **test-target infra quirks** (ts-sdk-events/testing have a `test` target but no vitest dep / no test files; ts-sdk-abha/tenant pass but the target exits non-zero) — area C `nx`-targets-drift. dev still `12963b72`. |
| 2026-06-22 | 3 | **Step 2 remediation — 5 modules + UM partial (blown through, incl. autonomous loop ticks).** Per module: deep-vet fix list → coherent fix → real-Citus DB tests → mutation-test the headline fix → INDEPENDENT adversarial review *before* accept → lint 0-err → per-module commit. **configurator `42fec70d`** (super-admin role gate + `client_secret` redaction on ABDM mgmt routes; tenant-scoped delete; real-DB provisionTenant saga + entitlement tests). **empi `502893fc`** (closed GET /patients empty-query **TENANT LEAK**; PatientSearchPage + limit-clamp; spec trim/document find-routes; real-DB tenant-isolation/dedup/pagination). **billing `d99724d0`** (ATOMIC payment numbering via `nextSequenceValue` + UNIQUE backstop migration `0002`; **PRE-TAX subtotal** fix [was double-counting tax]; string-money math; dead-code purge; payment-machine + real-DB tests). **record-foundation `8ce56210`** (idempotent care-context insert-or-catch-unique-violation; bundle FK pre-check→404; true `COUNT(*)` + opt-in paging; real-DB handler tests; adversary found+fixed a HIGH trim-divergence→500 + a silent-truncation regression). **registration `209ecb56`** (deleted dead non-compiling `router.ts`; surfaced 3 silently-swallowed intake failures via injected logger at all 3 `createVisit` sites; free-follow-up boundary tests; **P1 idempotency/saga/DB tests OUTSTANDING**). **user-management** `312fa15b` (`.cause` bug — below) + `f38004e8` (GET /providers error-envelope + spec doc; use-case-routing deferred as a product call). **CROSS-CUTTING FINDINGS:** (1) **deep-vet KEYSTONE was WRONG** — `ts-sdk-testing/createTestDb` (random schema + `SET search_path`) is UNSUITABLE for the schema-qualified Drizzle modules; real pattern = **`TEST_DATABASE_URL`-gated** (NOT app `DATABASE_URL` — vitest loads `.env`'s :5433 plain PG → `create_reference_table` errors, turning the suite RED) opt-in tests that drop literal schemas + apply the module's REAL migrations against `hims-verify` Citus :5444 (reference: `configurator-persistence.integration.test.ts`). (2) **drizzle-orm 0.45 WRAPS pg errors in `.cause`** — top-level `error.code` checks miss unique violations; UM's helper had this → its duplicate-username rollback was a silent no-op (fixed `312fa15b`: unwrap `.cause` recursively/cycle-safe). (3) **CI typecheck covers only 6/~25 projects** (area-C) — most modules carry ~18 latent type errors that slip CI; verify per-module via `tsc` filtering `TS6059`/`TS5101` config noise. **REMAINING:** registration P1; UM deep (real-DB provisioning/rollback/isolation tests + coherence-derive-from-spec + use-case-routing decision); pharmacy/integration-hub small items; master-data+opd (Python+ruff); cog-complexity refactor; Steps 3–5 (event-bridge/ports, BFF edge-auth, ruff). dev still `12963b72`. |
| 2026-06-23 | 4 | **Step 2 remediation — pharmacy P1 (real-DB upsert test + a confirmed latent bug).** The vet's one pharmacy fix-now: a real-Postgres test for `DrizzleDispenseRecordRepo.upsertForVisit` (the AUTHORITATIVE amount-recompute path, previously only mock-echo tested). Added `test/integration/data-access/dispense-record.repo.integration.test.ts` (4 cases asserting PERSISTED rows via raw `pool.query`: insert→repo-computed subtotal/total/line-billing; re-upsert→update-in-place + old lines deleted; 23505 retry; tenant-scoped unique index + reads). **Confirmed the predicted `.cause` bug is REAL** (probe: a 23505 from drizzle — plain OR inside `db.transaction` — is a `DrizzleQueryError` whose top-level `.code` is `undefined`; real code on `.cause` `DatabaseError`). pharmacy's inline `isPgUniqueViolation` (top-level only) thus NEVER fired → the concurrent-upsert retry was a silent no-op. Fixed: new local `src/data-access/postgres-errors.ts` (`.cause`-unwrapping, mirrors record-foundation/UM; ts-sdk-db consolidation = noted follow-up) wired into the retry catch; walk-in repo has no unique-catch so unaffected. **TEST-DESIGN LESSON (durable):** a naive `Promise.all` concurrency test catches this bug 3/3 in ISOLATION but **0/5 in FULL-SUITE** — once tests 1-2 warm Citus's cached plan the winner commits before the others reach INSERT, so the race serializes away (false confidence in the exact context CI runs). Replaced it with a DETERMINISTIC **held-lock** construction: a 2nd connection holds a conflicting (tenant,visit) row UNCOMMITTED (occupies the partial-unique-index slot, invisible under READ COMMITTED) → repo's INSERT blocks → poll `pg_stat_activity` for the Lock-wait (proves the repo passed the existence check) → COMMIT → forced 23505 → retry → UPDATE. Mutation-verified: held-lock test goes RED 3/3 in full-suite; deterministic unit test on the helper RED on the `.cause` cases. INDEPENDENT adversary (instrumented the lock-waiter to prove it's the repo's own INSERT) = **SOUND**; trimmed one speculative depth-boundary unit case per its nit. 56 pharmacy tests green; lint 0-err; 3 pre-existing latent `TS2xxx` in untouched files (walk-in repo, handlers) noted for the area-C typecheck-target work. P3 (PHARMACY_INTERNAL_API_KEY env policy) = walk-through. dev still `12963b72`. |
| 2026-06-23 | 4 | **Step 2 remediation — user-management P1 (real-DB persistence tests; validates the shipped `.cause` fix).** UM had ZERO real-DB tests (persistence/transaction/tenant-isolation only via mocks/in-memory doubles). Added `test/integration/data-access/user-provisioning.integration.test.ts` (5 cases, TEST_DATABASE_URL-gated, assert PERSISTED rows via raw `pool.query`): provisionUserWithAccess persists user + manual & role-template grants (grant_source/source_role_id distinction); **duplicate-username → DuplicateUsernameError** (this is the real-DB validation of the `312fa15b` `.cause`-unwrap — the in-memory double never wraps, so only a real driver 23505 exercises `isPostgresUniqueViolation`); **transaction atomicity** (a late FK failure rolls back the user row AND the earlier manual grant); per-tenant username uniqueness; **listEffectiveCapabilityKeys** snapshot-only + revoked-filtered + tenant-scoped. PURE new test (no source changed — the behaviors already shipped). Mutation-verified 3 ways (revert `.cause` unwrap → dup test RED; bypass `db.transaction` → atomicity RED; remove `revoked_at` filter → snapshot RED). INDEPENDENT adversary = SOUND-WITH-NITS; fixed both real nits: **[HIGH]** `beforeEach` TRUNCATE-CASCADE over 6 Citus *distributed* tables ran a ~5-9s multi-shard 2PC that straddled vitest's 10s hook timeout (flaky — passed by luck) → switched to per-statement `DELETE` children→parents (~100x faster; one `DELETE` per stmt because Citus forbids mixing reference-table [capabilities] + distributed-table DML in one tx); **[MEDIUM]** the snapshot "no-union" assertion was under-guarded (USER_A wasn't a ROLE_1 member, so a union-via-`user_roles`→`role_capabilities` regression stayed green) → made USER_A a ROLE_1 member with CAP_C template-only, re-verified the union mutation now goes RED. 238 UM tests green; typecheck (CI-gated) + lint 0-err. **DURABLE GOTCHA:** Citus multi-shard TRUNCATE in `beforeEach` is a flake source — use per-statement DELETE for real-DB resets. **UM REMAINING:** P3 derive-coherence-test-from-spec (replace the hand allowlist at openapi-runtime-coherence.test.ts:589-606 — next); /providers use-case-routing = product walk-through. dev still `12963b72`. |
| 2026-06-23 | 4 | **Step 2 remediation — user-management P3 (derive coherence test from spec; killed the allowlist that hid drift).** Replaced the hand-maintained 16-path allowlist in `openapi-runtime-coherence.test.ts` (which silently ignored any route not in the list — exactly how GET /providers slipped past) with a DERIVE-FROM-SPEC drift guard: extract documented paths from the spec text via regex (no YAML dep, same style the existing public-route check uses), normalize Fastify `:id`/OpenAPI `{id}` → `{}`, assert every PUBLIC runtime route is documented. **The stronger test immediately caught 3 MORE undocumented routes** beyond /providers — all `/internal/*` diagnostics (GET /internal/module-entitlements/{}, /internal/runtime-capability-catalog[/assignable]). DECISION: a principled `/internal/*` CATEGORY exemption (service-to-service/diagnostics = deliberately not in the public v1 contract; industry-canonical = keep internal routes out of the consumer spec), NOT a per-route allowlist — a new PUBLIC undocumented route still fails; the authMode loop still requires internal routes to be protected (no silent public-downgrade). Mutation-proven: un-documenting a public path (/auth/me) → test RED listing it. INDEPENDENT adversary = SOUND-WITH-NITS (all A–E confirmed; strictly stronger than the old loop; regexes verified collision-free; no type-silencing); fixed its INFO nit (authMode-loop comment overstated — one public /internal route [entitlement-cache invalidate, self-guarded by shared secret] exists but isn't mounted in this harness). 9 coherence tests + 238 UM tests green; typecheck + lint clean. **WALK-THROUGH / durable target (adversary-recommended):** the 3 internal routes should eventually get their own `user-management.internal.v1.yaml` + coherence test rather than an indefinite exemption (no internal-spec convention exists in the monorepo yet — architect/PO call). **Known scope: the guard is runtime⊆spec only** (stale-docs drift, e.g. conditionally-registered reset-password, not caught — acceptable for P3's undocumented-route goal). **UM fix-now items COMPLETE** (P1 `f6aae0ad`, P2 `f38004e8`, P3 here); only /providers use-case-routing remains as a product walk-through. dev still `12963b72`. |
| 2026-06-23 | 5 | **Step 2 remediation — registration P1 (the queued leftover: saga + idempotency + real-DB tests; + a 3rd `.cause` latent bug).** Closes the registration vet P1 (P2 dead-router + P3 swallowed-failures already done in `209ecb56`; free-follow-up + ensureEncounter-failure tests pre-existed there). **Found the SAME `.cause` bug a 3rd time:** `visit.repo.ts` AND `registration.repo.ts` guarded their idempotency 23505-retry with a top-level `err.code` check → drizzle 0.45 leaves it `undefined` → the retry NEVER fired (a lost insert race threw the raw error instead of replaying). Fixed via new local `src/data-access/postgres-errors.ts` (`.cause`-unwrap) wired into both catch blocks. Tests: `postgres-errors.test.ts` (unit, `.cause` unwrap); `create-intake-for-new-patient.test.ts` (13 EMPI-saga use-case cases vs fake repos/gateways — idempotent replay w/ no-EMPI-re-hit, duplicate→409 body, unavailable→503, error passthrough, abha_address strip-from-EMPI-body + merge-into-snapshot, ABHA-link-failure logging both flows, not-found/registration-missing throws); `data-access.integration.test.ts` (real Citus :5444, 12 cases — persistence, pre-check replay, **two held-lock 23505 races** [visit by idempotency key; registration via the distinctive `findByPatientId` fallback], per-tenant unique-index scoping, the free-follow-up support queries `countFreeFollowUpVisits`/`findLatestByPatientAndDepartment`, abha lookup). **Both repos in ONE integration file** — two files race on the shared `registration`/`drizzle` schemas under vitest file-parallelism (the 2nd worker's beforeAll DROPs mid-run of the 1st). Mutation-verified: revert `.cause` recursion → exactly the 2 `.cause` unit cases + 2 held-lock tests RED; un-strip abha_address → the saga strip-test RED. **INDEPENDENT 3-lens Workflow adversary** (test-sincerity SOUND — empirically reproduced the held-lock + `.cause` bug via probes; completeness SOUND — every contract item mapped to a sincere test; correctness MINOR) → acted on its required nit (rewrote the misleading file-header comment) + closed the symmetric LOW gap (existing-patient ABHA-link-failure test). 54 registration tests green (incl. 12 real-DB); lint 0-err; typecheck clean (only pre-existing TS5101 baseUrl-deprecation config-noise). Commit `9a2b494c`. **NEW TRACKED FOLLOW-UP (D-item, the adversary's MEDIUM):** there are now **4 near-identical `postgres-errors.ts` copies** (pharmacy/record-foundation/UM/registration) already DIVERGING (UM = `isPostgresErrorCode`+FK helper; others = `hasPostgresCode`) — promote to **`@hims/ts-sdk-db`** (`src/errors.ts`, re-export from index; every module already depends on it) and delete the 4 copies + reconcile UM's API. Deliberately deferred to keep this P1 commit scoped (reviewer agreed deferral is defensible). dev still `12963b72`. |
| 2026-06-23 | 6 | **Step 2 cross-cutting — postgres-errors → `@hims/ts-sdk-db` consolidation (the session-5 D-item, DONE same day, commit `739b6283`).** Promoted the `.cause`-unwrapping helpers into `packages/ts-sdk-db/src/errors.ts` (`isPostgresUniqueViolation` + `isPostgresForeignKeyViolation`, re-exported from index) and DELETED the 4 diverging per-module copies (pharmacy/record-foundation/user-management/registration) + the 2 redundant unit-test copies. Every module already depends on `@hims/ts-sdk-db`, so this is a shared-SDK helper, NOT a forbidden modules/*→modules/* import (the per-module file-header comments had misleadingly implied otherwise). 8 source files repointed their import (call sites unchanged); one contract test kept (registration/test/unit, repointed to the SDK + expanded to cover both wrappers — stays consumer-side because **ts-sdk-db has no vitest target yet**, the separate area-C SDK-test-infra item). **Pure mechanical relocation, identical logic, zero behavior change** — verified across all 4 consumers: pharmacy 51 / registration 56 / record-foundation 20 / user-management 233 tests green (incl. real-DB held-lock 23505 + dup-username rollback that exercise the helper end-to-end); UM typecheck (CI-gated) + `tsc` on ts-sdk-db/registration/pharmacy/record-foundation clean; lint 0-err; no dangling imports. Net −129 lines (4 copies+2 tests → 1 helper). Given it's a verified mechanical refactor (risk surface = missed call site / dangling ref / behavior change, all covered by the 360 passing tests + typecheck), a full 3-lens adversarial workflow was not run — proportionate self-verification instead. **This closes the recurring `.cause` bug class for good: the next module that needs unique/FK-violation handling imports the `.cause`-aware helper from the SDK.** dev still `12963b72`. |
| 2026-06-23 | 8 | **D22 cog-complexity — UNTESTED web `.tsx` tail (user decision "a": decompose w/ rigorous JSX/hooks-equivalence review).** Ground-truth rescan: backend = **0** cog-complexity warnings (fully done); entire remaining tail = **32 warnings across ~23 web files** (CC up to 71), all UNTESTED React components/routes + the `use-abha-wizard` hook (only `um-permissions.test.ts` exists in web/src, a `.ts` lib test). Verification net per untested file = (1) `eslint --rule cognitive-complexity:error,15` = 0 + per-rule error-count diff vs HEAD (via `eslint --stdin-filename` on `git show HEAD:`) = no new errors; (2) `tsc --noEmit` per-file error-count diff vs a captured baseline (eslint can't see types); (3) independent React-aware adversarial review (hooks order/unconditionality, module-scope component identity, render-tree/keys, prop-type fidelity, no metric-gaming). Ran as decompose→adversarial-verify Workflows in 3 waves, gated + committed per wave. **D22 COMPLETE — all 23 web files done + rule flipped.** **Batch 1 `2cbf2482`** (5 worst: role-management-sections RoleEditorDialog CC71 + CapabilityTreeNodeRow CC19 → 11 module-scope subcomponents + pure flag-helpers; form-table cell-renderer CC37 → FormTableCell<T>; frontdesk/visit-registration route CC33/20/17 → .panels.tsx; tenant-detail-panels CC31/18; role-management-panel CC28/19). **Wave A (batches 2+3) `1153cc42`** (10 files incl. create-user-form CC25, edit-user-dialog, generic-nav-node, abha-wizard-login-steps, tariff-master route, data-table, use-abha-wizard HOOK, tenant-details-edit-panel, admin-dashboard, registration-reports-modal). **Wave B (batches 4+5) `bd56f0e3`** (8 files incl. visitpad/vitals, tariff-service-form-fields, visit-registration-sections, create-tenant-wizard [extracted validateWizardStepAdvance/buildTenantOnboardingPayload into wizard-helpers.ts — covered by wizard-helpers.test.ts, 20 tests green], create-rx/page, configurator tenant.index + tenant.$organizationId routes, sidebar-nav-link). **23/23 adversarial verdicts PASS.** My independent gate (eslint per-rule HEAD-diff + tsc baseline-diff) caught + I fixed **2 introduced type regressions the reviewers missed** (visit-registration `string\|null`vs`string\|undefined` prop; create-user-form 4 RHF resolver-transform errors → fixed via narrow UseFormSetError/SetValue/GetValues capabilities + one documented validated-values narrowing) — eslint can't see types, so tsc-diff is load-bearing. **Final state: full-repo cognitive-complexity@15 = 0 violations; tsc src total = 246 (unchanged baseline).** **Rule flipped `<this commit>`:** `sonarjs/cognitive-complexity` → `['error', 15]` + added `max-lines: ['warn', 500]` (45 oversized-file warnings, non-blocking — no `--max-warnings` anywhere); nx lint green post-flip (verified ts-sdk-db). **ENFORCEMENT CAVEAT / EXPANDED FINDING:** error@15 is CI-enforced on the **25 nx-linted backend projects** (modules/* + packages/* + opd-svc, all 0 violations) but **NOT on `services/web` — web has NO nx lint target AND no typecheck target** (no explicit, none inferred), so CI never lints OR typechecks the frontend at all. The web decomposition is enforced only via editor/direct-eslint until a web lint+typecheck gate is added. `tsc --noEmit` surfaces **246 pre-existing src + 37 test type errors** (baseline captured). **Distinct future stream: add `services/web` lint + typecheck nx targets and drive both to 0** (large — would surface the pre-existing web error backlog). dev still `12963b72`. |
| 2026-06-23 | 7 | **Big autonomous cleanup run (ultracode/workflow-orchestrated) — master-data P1, #2/#3/#4/#5 + D22 cog-complexity 45/88.** Per-module remaining-vet items + the user's 6-decision batch. **master-data P1 `7959b9a9`** (suite 4-failed→113: relaxed visitpad unit `code` validation — added `VISITPAD_UNIT_CODE_PATTERN {1,9}` since a units catalog must accept 'g'/'kg'/'lb' and the conversion schema already used min_length=1; fixed FakeDepartmentRepository to populate DB-generated cols). **#2 opd canonical-API ANALYSIS `a0c0d4b7`** (2-reader workflow → `docs/architecture/cleanup/opd-prescription-api-comparison.md`; recommend keep normalized REST family, demote JSONB to a thin ABDM-M2 trigger adapter; **AWAITS user cutover call**). **#3 RESOLVED** (master-data is ALREADY a `type:service` w/ serve+deploy:aks — D5 "split" moot, only cosmetic modules/-vs-services location; deferred per user's modules-first direction — see composition design follow-up). **eslint denoise `39c78fdb`** (ignore `.venv`/`.claude` worktrees/`*.generated.ts`/`*.gen.ts` — flat config doesn't honor .gitignore). **#5 integration-hub lint `48dc7613`** (scope widened src/index+lib→`src/**`; 28 surfaced errors cleared: stripTrailingSlashes DRY helper [12 trailing-slash regexes], 8 mechanical, 7 justified security disables [linear regexes / ABDM-protocol MD5 / FHIR system-URIs — all adversary-challenged, none rejected], 1 real dead-ternary bug; 139 tests green). **#4 per-module-DB integration-tests-in-CI `8e0c9a59`** (area-B/#36): each of the 7 modules got a `test:integration` target (`vitest run test/integration`, TEST_DATABASE_URL→own `hims_test_<module>` DB, base overridable via `TEST_DATABASE_BASE_URL`); CI Stage 5 creates the 7 DBs+citus on the existing Citus service then `nx affected -t test:integration`. Per-module DBs because every harness drops the shared `drizzle` schema in beforeAll. Verified locally: registration(12) solo + pharmacy(4)/empi(4)/record-foundation(7) CONCURRENT, all green. **D22 cog-complexity DECOMPOSITION — 45/88 DONE** via 6 decompose→adversarial-review workflows (all SOUND, behavior-preserving, NOT metric-gaming, per-file/test gated): b1 `3b32ba42`(6), b2 `c5d2c695`(8), b3 `7877e0fb`(10), b4 `fa42e28c`(9) = **ALL BACKEND done (~33)**; web-logic b1 `dc51562e`(6) + b2 `7dc43cdf`(6) = **ALL pure web `.ts` logic done (12)**. Worst tamed: um-svc authz-target-resolver CC 85→~3 (order-independent ROUTE_TABLE), clinical-report-print 56→<15. **BLOCKED on remaining 22 web `.tsx` COMPONENTS (CC up to 71) + the `use-abha-wizard` hook + the `error@15` rule-flip: ALL 22 .tsx are UNTESTED → cannot safely decompose autonomously (no test net, only JSX-diff review). Pinged user ×2 for the decision: (a) decompose untested .tsx w/ rigorous JSX/hooks-equivalence review, (b) carve a `services/web` cognitive-complexity exception, or (c) ratchet.** Also noted: pre-existing `services/integration-hub-svc/src/main.ts:46` lazy-import lint error (structural, separate). dev still `12963b72`. |
| 2026-06-23 | 9 | **opd #2 consolidation — STEP 1 (tenant gap) + STEP 2 (ABDM-M2 safety net).** Executing the user's cutover decision on the session-7 comparison doc: *consolidate to ONE normalized `/prescriptions` API and port the JSONB FHIR-bundler + ABDM-M2 trigger onto it*. Sequenced per the doc's reversible-hedges (re-point is high-risk; ABDM is the integration USP). **STEP 1 `971178d4`** (was unlogged): closed the normalized family's tenant TRUST gap — `tenant_id`/`doctor_id` now resolved from `iq_tenant_id`/`x-user-id` headers via `require_tenant_id`/`resolve_doctor_id` (dropped from query/body), threaded create(tenant_id, doctor_id, payload) → service → repo → port; new `test_prescription_api_tenant_security.py` proves cross-tenant B can't read/mutate A's row (404/empty) AND A stays untouched DRAFT, + missing/invalid-header 400s. **STEP 2 `<this commit>`** — the safety net the re-point is gated on (doc §4 risk#1 + §5 hedge#2 demand a real/contract test BEFORE re-pointing). Found the pre-existing `test_abdm_m2_multi_hi_type.py` mocks EVERY `_persist_*` → the actual HTTP payloads to Record Foundation + integration-hub were NEVER asserted. New **`test_abdm_m2_wire_contract.py`** (7 tests) drives the REAL pipeline (`trigger_m2_after_end_consultation` → `persist_visit_abdm_bundles` → real `_persist_*` → real `fhir_bundle_mappers` → real `hims_sdk_fhir` builders), mocking ONLY genuine boundaries (snapshot loader, the two HTTP transports `_http_json`+`urlopen`, PDF render, blob repo). Deterministic, no real DB (SDK mints non-deterministic ids → assert stable facts: literal care_ref formats, NRCES profile URLs, resource types, clinical-content strings via `json.dumps`, + ABHA address/practitioner-reg-id/visit_number/HI_TYPE wire literals). **Mutation-tested (mine + reviewers'): 10-mutation matrix A–J all RED appropriately** (care_ref suffix→3 RED; drop immunization→4 RED; redact diagnosis/medicine via REAL mapper→3/1 RED; status/source_system/profile/gate/hub-missing/PDF→RED) — a real net, not a skeleton. **3-lens adversarial review Workflow** (integrity=SOLID no tautologies; repoint-skeptic=SOLID-WITH-FIXES; simplicity reviewer hit the StructuredOutput cap → applied own simplicity judgment). Acted on both MUSTs: (1) **docstring HONESTY** — corrected the overstated "survive re-point UNCHANGED / byte-for-byte" claim; it is a PERSIST-SIDE anchor only (the mocked `_load_visit_clinical_snapshot` is exactly what step 3 rewrites), so sourcing-equivalence is step 3's REQUIRED complementary test; (2) closed an IN-SCOPE silent gap (ABHA/practitioner-reg-id asserted nowhere → added, verified they reach the bundle). **Verified:** opd suite **83 passed, 5 failed** (the 5 failures are pre-existing JSONB `test_prescriptions.py` real-Postgres `FROM registration`-schema-not-migrated, unchanged); ruff clean on the new file. **Steps 3–6 pending (high-risk, GATED):** (3) re-point trigger to source from normalized `PrescriptionModel` + step-3 sourcing-equivalence test (seed normalized rows → REAL loader → same care_refs/content); (4) port good JSONB behaviors (idempotent bootstrap, visit FSM, nurse pre-consult, patients queue); (5) FE end-consult → normalized `/finalize`, retire `isPatientScopedOpdRoute` split-brain; (6) backfill→verify→DROP `form_data` (flag for careful/user pass; gate drop on the real ABDM test). **DEFERRED to step 3 (defer-with-a-gate):** capture a golden of current `effective_form_data` output BEFORE the re-point — needs real-Postgres (step 3 needs it anyway) and the JSONB path stays live until step 6, so the artifact isn't lost. dev still `12963b72`. |
| 2026-06-24 | 9 | **opd #2 consolidation — STEP 3 (re-point ABDM-M2 to the normalized aggregate).** The high-risk re-point the step-2 wire test was gating, executed per doc §4 step 2 + §5 hedge 2 (reversible: two source files, no column drop/migration/FE change). New **`build_form_data_from_prescription_model(rx)`** (`prescription_form_data.py`, pure, no DB I/O) maps the eager-loaded `PrescriptionModel` aggregate → ABDM/FHIR form_data, covering exactly the keys the mappers + content-gates read — **strictly more complete** than the legacy `effective_form_data` 5-table reconstruction (now also carries **diagnosis, allergies, care-plan** which it silently dropped). `_load_visit_clinical_snapshot` now loads `PrescriptionRepository.get_by_visit_id` (all 18 children `selectinload`ed — method already existed) for BOTH identity AND clinical, replacing the JSONB `get_prescription_by_visit_id`/`effective_form_data`; `effective_form_data` stays for the JSONB read/PDF paths (5 tests + 2 handlers) — only the M2 trigger stopped using it. **Two deliberate calls, both adversarially verified TRUE:** (D-a) **keep the FE override-merge** — the JSONB end-consult write path syncs only `legacy_vitals`+`vaccines` to child tables, so blob-only complaints/meds/dx must still merge over the normalized base until the FE cutover (Option A would regress the live flow); (D-b) **reuse the `__hims_immunization_v1:` decoder** rather than delete it per doc step 2 — `prescription_vaccines_required` has no manufacturer/lot/dose columns, so deletion now = data loss (deferred-with-a-gate; recorded in comparison-doc §6 + the fn docstring). New **`test_abdm_m2_sourcing_equivalence.py`** (7 tests) drives the REAL sourcing chain (seed normalized via `repo.create` on SQLite → REAL `_load_visit_clinical_snapshot` → REAL mappers/builders → capture RF POSTs), no-override branch; load-bearing asserts are **diagnosis + allergy** (only satisfiable by sourcing from normalized children). **Mutation matrix A–E all RED appropriately** (neuter base→4 RED; drop dx loop→2 RED; drop immun loop→2 RED; ignore override→override-test RED; skip soft_delete→soft-delete-test RED). **4-lens adversarial-review Workflow** (correctness/simplicity/test-integrity/plan-reversibility) — all SOUND, the test-integrity lens independently re-ran mutations A/B/C and confirmed RED; 0 critical, 1 major (refuted on verify), rest minor/nit. Acted on the worthwhile findings: dropped dead `quantity` (Decimal-serialization landmine, violated the builder's own no-dead-data rule); documented the deliberate `deleted_at`-filter semantics change (soft-deleted rx now excluded from M2) + locked it with a test; added the previously-uncovered **override-branch** coverage test (D-a) + parameterized the harness; logged both deferred gates in comparison-doc §6. Cleared the reviewers' stale `.pyc` (the documented stale-artifact landmine — root cause of a one-time flake they saw) and confirmed determinism (15 ABDM tests ×5 runs green). **Verified:** opd suite **90 passed, 5 failed** (same pre-existing JSONB `test_prescriptions.py` real-Postgres failures, unchanged); wire-contract test passes UNCHANGED; ruff 0 new errors (only pre-existing E501 backlog). **Steps 4–6 still pending** (port good JSONB behaviors; FE → normalized `/finalize`; gated backfill→verify→DROP `form_data`). dev still `12963b72`. |
| 2026-06-24 | 9 | **opd #2 consolidation — STEP 4 VERIFICATION (scope correction, no code change; doc-only commit).** Before building "step 4 = port four JSONB behaviors with new endpoints," re-read the whole opd module + the frontend and put the conclusion through an adversarial refutation Workflow (`wf_fbcb7d27`, 4 refuters + completeness critic). **The §4 step-4 framing was partly WRONG.** Verified (all with file:line evidence): **(a) idempotent bootstrap** already done client-side by the FE (`bootstrapOpdPrescriptionForVisit` = GET by-visit→else POST /prescriptions); **(b) visit-status FSM / derived UI status** derives purely from normalized child-table content (`PrescriptionModel` has no `form_data` attr → `prescription_form_data_has_content` runs the child-exists check → `effective_encounter_status(visit=None,…)` yields registered/in_progress/completed) — **no `opd.visits` row needed**; **(d) patients queue** already normalized (doctor+nurse queues both use `listRegistrationVisits` + the normalized `/prescriptions/by-visits` overlay; JSONB `GET /opd/patients` + `GET /visits` have **zero live callers**); **(c) nurse pre-consult** is the one live JSONB call but composes from normalized create+update. ⇒ **NO new normalized endpoint is needed (narrow claim SOUND).** The complete live FE→JSONB surface (the step-5 cutover target) is exactly 4 endpoints: PUT/POST /visits/{id}/prescription[/end], GET /visits/{id}/prescription, PUT …/pre-consult. **But the critic REFUTED the broader "step 4 = pure FE rewire + gated retire" as incomplete + UNSAFE-as-ordered:** (1) two FE-edited fields have **no normalized column** and are silently dropped on the normalized round-trip — `medicalHistory.dietType` + `imagingRequired[].byWhen` (need additive columns, fold into the backfill); (2) **CLINICAL DATA-LOSS GATE (unrecoverable):** the legacy JSONB write only ever synced `legacy_vitals`+`vaccines` to child tables, so for every JSONB-written row **diagnosis/medicines/allergies/care-plan live ONLY in `form_data`** — a normalized GET→edit→finalize of such a row would silently drop them ⇒ **the form_data→child backfill must gate the FE cutover (step 5), not just the column drop (step 6)**; (3) `pre_consulted` is a permanent semantic loss on normalized-only (survivable only because the FE collapses in_progress==pre-consulted) → document as a deliberate decision; (4) add a read-then-edit-legacy-row regression test before any FE rewire; (5) the ABDM-M2 `form_data_override` removal must FOLLOW the backfill (the workflow MAP had it inverted). Also produced the full backend retirement map: it's ONE interleaved alembic DAG (not two lineages); `prescription_form_data.py` must be **split/rewired not deleted** (keep `build_form_data_from_prescription_model`/`_merge_form_data`/`_vaccine_db_to_immunization_row`/`effective_form_data_for_prescription` normalized path); shared-keep list = `Visit`, visit_status resolvers, abdm_m2, clinical_documents_service, pharmacy normalized-finalize notifier, HealthDocument* schemas. **All captured in comparison-doc §7 (verified scope + safety-ordered sequence).** Decision: the remaining opd consolidation is a **data-sensitive gated migration + a user-visible FE cutover with a patient-safety gate** — held for a careful/user-attended pass (per the user's own step-5/6 flags); did NOT build speculative endpoints (would violate simplicity equilibrium). **Autonomous loop pivots to the safe fallback = the ruff phase** (opd + master-data Python lint → 0). Commit: doc-only (comparison §7 + this row). dev still `12963b72`. |
| 2026-06-24 | 9 | **RUFF PHASE — COMPLETE (opd `12ecab1d` + master-data `52943f92`).** The designated Python-lint stream (the opd-step-4 safe fallback). Careful per-module, NOT blind `--fix`. **opd 93→0:** B008 (FastAPI `Query()`/`Depends()` in arg defaults) fixed via `[tool.ruff.lint.flake8-bugbear] extend-immutable-calls` whitelist (the FastAPI+ruff canonical fix, not handler rewrites); F821 (the `rx: Prescription` forward-ref strings in visit_status.py) via a TYPE_CHECKING-only import; F841 unused test binding removed; 39 safe autofix (I001/UP007/UP035/F401[verified no public re-exports]/F541); 51 E501 hand-wrapped via a per-file Workflow (20 agents, one per file). **master-data 233-OURS→0:** B904 → `raise HTTPException(...) from None` (caught ValueError is input-validation noise); I001(51)+F401(27) autofix (also cleared 18 E501 incidentally); 136 E501 via a 59-agent per-file Workflow. **EXCLUDED the not-ours untracked `alembic/versions/026_user_management_catalog_seed.py`** (7 errors not ours — left byte-identical, md5 `5e51ab2a…` verified before+after, never staged). **Gate (both):** `ruff check` = 0 (master-data excludes 026); pytest = opd 90/5-preexisting (the 5 are environmental — test_prescriptions.py hits real Postgres :5433 `relation "visit" does not exist`, confirmed pre-existing via stash-and-rerun) + master-data 113 passed; string/SQL splits verified byte-identical (implicit-concat with trailing-space boundary; SQL wrapped at whitespace only). Lint-only, zero behavior change. **Process LESSON:** Workflow `args` (a JSON array) arrived empty as the script global on first launch (→ 0 agents, no-op); re-launched with the file list embedded as a script const. **REMAINING big streams (architectural/large, each a fresh focused turn):** event-bridge facade (D8) + ports/adapters (D3); BFF edge-auth (D13); `services/web` lint+typecheck nx gate (~246 pre-existing web tsc errors). dev still `12963b72`. |
| 2026-06-29 | 9 | **opd #2 consolidation — REFACTOR-STEP-1 (parity columns), commit `10a1efbe`.** Post the user's no-data confirmation (app never went live → backfill MOOT → straight refactor), the first increment of the §7 3-step refactor. Two Create-RX form fields had no normalized column and were silently dropped on a `/prescriptions` round-trip (lived only in the JSONB `form_data` blob): `medicalHistory.dietType` → new `prescription_medical_histories.diet_type` (String 64); `imagingRequired[].byWhen` → new `prescription_ordered_imaging.when_text` (String 256, the form's FREE-TEXT "By When" — distinct from the typed `due_by` the OPD form never populates; `due_by` kept untouched). Wired end-to-end: ORM model, **Alembic `0005`** (Postgres-only/idempotent; SQLite tests build columns from the ORM via `create_all`), pydantic request+response schema, repo `_apply_clinical` write path, response mapper, FE write+read mapper (`opd-prescription-mapper.ts` — stopped hardcoding `dietType:''`/`byWhen:''`), FE payload type, `specs/openapi/opd.v1.yaml`. **Tests (real, fail-on-pre-change):** BE create+update round-trip on SQLite (`test_prescription_repository.py` +2) → opd **92 passed / 5 pre-existing real-Postgres** (was 90+5); FE mapper round-trip (`opd-prescription-mapper.test.ts` +1) → 10 passed. Module ruff clean; ESLint clean on changed FE files. **Caught+reverted** an inert edit to dead code (`clinical_payload_to_form_data`, 0 callers) and correctly **did NOT** touch `build_form_data_from_prescription_model` (ABDM/FHIR source — no FHIR mapper reads diet, confirmed). **INDEPENDENT adversarial review = SHIP** (round-trip complete, tests real & fail-pre-change, migration head/idempotency/types sound, `when_text` design justified, no over-engineering). **CARRIED-FORWARD GATE → fold into refactor-step-3 (JSONB retire):** the create/update→read round-trip is lossless, but the clinical-report/PDF bridges (`_clinical_to_form_data` + `_load_normalized_clinical_impl` MH SELECT in `prescription_form_data.py`; the dead `clinical_payload_to_form_data`) must project `diet_type` BEFORE `form_data` is dropped, else diet vanishes from PDFs (imaging `byWhen` never reached PDFs — pre-existing). **NEXT = refactor-step-2 (FE cutover):** rewire the 4 live JSONB flows (save-draft / end-consult / read / nurse pre-consult) to the normalized family + retire `isPatientScopedOpdRoute`, ship FE+BE together (CAUTION: web has no lint/typecheck/test gate + non-green baseline → review JSX/behavior + run the specific related tests). dev still `12963b72`. |
| 2026-06-29 | 9 | **opd #2 consolidation — REFACTOR-STEP-2 (FE cutover), FE-only commit.** Rewired all four live FE flows off the JSONB `/visits/{id}/prescription[/end|/pre-consult]` family onto the normalized `/prescriptions` family and deleted the JSONB-only FE code. `opd-prescription.ts`: `fetchPrescriptionByVisitId` → `GET /prescriptions/by-visit/{visit}`; `saveOpdPrescriptionDraft` collapsed to `existingId \|\| ensureDraft(visit,patient)` → `updatePrescriptionClinical`; `endConsultation` inlined to ensure→update→`finalize` (fires pharmacy-notify + ABDM-M2) → `updateRegistrationVisitStatus`. Nurse: `saveNursePreConsult(visitId, patientId, formData)` delegates to the normalized draft save (panel sources `patientId` from store `context.patient.id`; inline-vitals from its prop). **Deleted** (zero callers verified repo-wide): `isPatientScopedOpdRoute`, `upsertVisitPrescriptionFormData`, `endOpdPrescription`, `endOpdConsultation`, `OpdVisitPrescriptionResponse`, `visitPrescriptionResponseToSession`, dead `ensureOpdRegistrationEncounter` alias; also dropped the redundant nurse pre-sanitize (the mapper already sanitizes). **Tests:** replaced the stale `isPatientScopedOpdRoute` test with sincere rewired-flow tests + new nurse test (mock only `@/lib/api-client`/registrations/tenant/auth; real mapper) — **proven to fail on the old JSONB source** in a HEAD worktree (`unexpected apiClient call: …/visits/…`). create-rx+nurse **54 passed**; broader sweep (create-rx/opd-patients/nurse/frontdesk) **108 passed**. Lint: 0 new (fixed 3 pre-existing `preserve-caught-error` via `{cause}`, cleared 1 unused-var; 4 remaining are pre-existing untouched-logic = step-3). **3-lens adversarial review** (`wf_8011ca8d`) = 2× SHIP_WITH_FIXES + 1× SHIP; behavior-parity independently confirmed the `pre_consulted` design-loss is fully absorbed by the queue overlay; all requested coverage added. Net −95 lines on the main file. **THREE CONFIRMED gates folded into refactor-step-3** (see comparison-doc §7 G1–G3): **G1** nurse-initiated `doctor_id` now = acting nurse (old JSONB used registration-assigned doctor) → backend doctor-attribution policy, **gate before real ABDM-M2 go-live** (FHIR practitioner identity); **G2** retire the whole dead-code island (`listOpdVisits*`, `OpdVisitSummary`, `fetchPrescriptionStatusesByVisitIds`, `opd-visit-status` overlay) with the JSONB family; **G3** strip vestigial `?tenant_id=`/body `tenant_id`/`doctor_id`. **NEXT = refactor-step-3 (retire JSONB family + DROP `form_data`)** — honor G1–G3 + the step-1 PDF/diet gate. dev still `12963b72`. |
| 2026-06-29 | 9 | **opd #2 consolidation — REFACTOR-STEP-3 PREP (the two non-destructive, autonomously-safe pieces), commits `80ef7596` (a) + `80cef22a` (b).** Did the safe prep so the eventual destructive `DROP form_data` is de-risked, holding the JSONB deletion + irreversible drop for a careful/user-attended pass. **(a) RESOLVES the step-1 PDF/diet gate:** re-pointed the clinical report/PDF service (`clinical_documents_service.py`, both call sites) from `effective_form_data` (legacy blob over a PARTIAL reconstruction that silently dropped diagnosis/allergies/care-plan/diet/imaging/tests/procedures for normalized-written rows) to the COMPLETE typed converter `clinical_payload_to_form_data(prescription.clinical)`, and added the missing `dietType` mapping. Fixes a latent report-incompleteness bug live since the STEP 2 cutover AND removes the report's `form_data`-blob dependency. Imaging `byWhen` NOT carried (pre-existing `_map_imaging` gap); diet NOT added to the ABDM structured builder (no FHIR mapper reads it → dead data per contract). **(b) RESOLVES gate G1** (user decision, matches existing prod): `/finalize` stamps `rx.doctor_id` = the finalizing actor (trusted `x-user-id` via `resolve_doctor_id`), overriding the draft creator → ABDM-M2 FHIR practitioner = the finalizer; guarded so the nil `SYSTEM_DOCTOR_ID` fallback never clobbers a real prescriber. **Tests (sincere, fail-on-pre-change):** converter carries diet + full sections; `_map_medical_history` surfaces diet; finalize override to a different id; nil-finalizer keeps the prescriber (guard); HTTP boundary create-as-A→finalize-as-B. opd **97 passed / 5 pre-existing** real-Postgres; ruff clean. **Independent 2-lens adversarial review:** report-parity = SHIP (strict improvement); attribution+test-integrity = SHIP_WITH_FIXES (MAJOR nil-clobber fixed). **STILL PENDING for gated step-3 (c)/(d):** G2 dead-code island, G3 vestigial inputs, remove abdm_m2 `form_data` override branch (now unblocked), retire the JSONB family, and the irreversible **`DROP opd.prescriptions.form_data`** + alembic-lineage collapse — flagged for a careful/possibly-user-attended pass. dev still `12963b72`. |

---

## 12. Phase 1 — proven drizzle-kit migration recipe (empi reference, VERIFIED)

Verified end-to-end on `empi` against real Citus 12.1 (2026-06-21): fresh apply, idempotent re-run, 7 tables distributed by `iq_tenant_id`, pg_trgm + gin index, journaled tracking table. **This is the canonical recipe for the remaining 7 TS modules.**

**Shared infra (DONE):** `packages/ts-sdk-db` now exports `applyMigrations(connectionString, migrationsFolder, { migrationsSchema, migrationsTable })` (runtime `migrate()` from `drizzle-orm/node-postgres/migrator`) and `createPool`. Stale on-disk `.js`/`.d.ts` build artifacts under `ts-sdk-db/src` were deleted (they shadowed the `.ts` under tsx).

**Tracking-table decision:** schema **`drizzle`** (drizzle's own, auto-created — avoids the fresh-DB chicken-egg of the module's own schema, since the generated baseline emits a bare `CREATE SCHEMA "<mod>"` with no `IF NOT EXISTS`), table **`__drizzle_migrations_<module>`** (module-unique — the node-postgres migrator advances by latest-applied timestamp, so a *shared* table makes modules skip each other's pending migrations).

**Extensions + Citus = journaled `--custom` migrations** (issue #66 default), NOT infra bootstrap — so they run wherever `applyMigrations` runs (dev/CI/prod). Journal ⇒ exactly once ⇒ no `DO $$` / `IF NOT EXISTS` / `pg_dist_partition` guards needed.

**Per-module steps:**
1. `drizzle.config.ts`: `schema: ./src/schema/tables.ts`, `out: ./migrations`, `dialect: postgresql`, `schemaFilter: ['<schema>']`, `migrations: { schema: 'drizzle', table: '__drizzle_migrations_<module>' }`.
2. Delete hand-written `migrations/*.sql` + any old `migrations/drizzle/`.
3. Generate fresh, in order: `drizzle-kit generate --custom --name <ext>` per extension (edit → `CREATE EXTENSION IF NOT EXISTS ...`) → `--name init` (baseline) → `--custom --name distribute_citus` (edit: `create_distributed_table('<schema>.<t>','iq_tenant_id')` for tenant tables; **`create_reference_table('<schema>.<t>')` for catalog/control-plane tables FK'd by distributed tables — #92**). Extension must precede any index that needs it.
4. Rewrite `src/schema/apply-migration.ts` → call `applyMigrations(conn, MIGRATIONS_DIR, { migrationsSchema:'drizzle', migrationsTable:'__drizzle_migrations_<module>' })`; **keep the exported `applyXSchemaMigration` name** so boot (`services/*-svc/src/main.ts`) + `scripts/apply-migration.ts` callers are unchanged.
5. `project.json`: remove duplicate `db:migrate` (keep `db-migrate` + `db-generate`).
6. Verify against throwaway Citus: `docker run -d --name hims-verify -e POSTGRES_USER=hims -e POSTGRES_PASSWORD=hims -e POSTGRES_DB=hims_dev -p5444:5432 citusdata/citus:12.1` then `CREATE EXTENSION citus;` — apply ×2, check tables + tracking rows + `pg_dist_partition`. (Dev compose `infra/docker/docker-compose.yml` had a WSL2 bind-mount glitch on `citus-init.sql`; throwaway container sidesteps it.)

**Per-module notes for the fan-out:**
- billing, registration, pharmacy, record-foundation: tenant tables → distribute by `iq_tenant_id` (like empi).
- configurator, user-management: catalog/control-plane tables (modules, permissions, system_roles, organizations, tenants, capabilities) FK-referenced by distributed tables → **`create_reference_table`** (#92), not distribute.
- user-management `auth` schema (better-auth): non-distributable (text PKs) — document as a reference/local exception (ties to AuthN phase, D-decisions there).
- integration-hub: schema lives at `src/integrations/abdm/schema/tables.ts` (not `src/schema/tables.ts`) — adjust config path.
- Extensions seen: pg_trgm (empi). Check each for pgcrypto/`gen_random_uuid` (PG17 has it built-in — verify per image).

**Schema-parity follow-up (surfaced by the fan-out — HIGH priority):** the fresh `tables.ts` baselines exposed that the old hand-written SQL carried integrity objects **not present in `tables.ts`** (the designated source of truth), so the new baselines omit them. Restore them into each `tables.ts` (`.check()` / `foreignKey()` / partial+unique indexes) and regenerate: **configurator** — 4 FKs (`tenants.org_id→organizations`, `tenants.parent_tenant_id`, `tenant_integration_profiles→tenants`, `tenant_api_keys→tenants`); **pharmacy** — CHECK constraints (gender enum, non-negative numerics, dispense_status / walk-in-vs-visit mutual-exclusion) + partial/unique indexes; **record-foundation** — 3 CHECKs (source_origin, status) + composite FK `bundles→care_contexts`; **user-management** — partial index `idx_users_api_key_prefix`. This closes the audit's "tables.ts vs SQL drift, no gate" gap; afterward add a CI `drizzle-kit generate` empty-diff check so it can't recur. (billing, registration, integration-hub: no drift — `tables.ts` already complete.)

**Phase 1 Python/DB reshape — DONE 2026-06-21 (commit `41926d6f`, verified on real Citus).** OPD `iq_tenant_id` (migration `0004`); master-data #91 rename (`044`) + #92 reference tables; `citus-init.sql`→extensions-only + WSL2 dir-mount; unified `DATABASE_URL` (pydantic fallback + psycopg normalization) + collapsed per-module k8s secrets; Makefile single-pass; `database-principles.md §2`.

**Phase 1 deferrals → Phase 2+ follow-ups (file as issues):**
1. **OPD Citus distribution** — OPD tables stay LOCAL. Blocked by `opd.visits` PK being `(id)` only (needs `(iq_tenant_id, id)`) + a legacy-vs-normalized duplicate mapping of `prescriptions` (`prescription_row.py` LegacyBase vs `prescription/prescription.py` Base). Gate: distribute only after that structural reconciliation.
2. **`master_tenant` platform tables as reference tables** — migration 012 makes them `create_reference_table` despite each carrying `iq_tenant_id`; should be `create_distributed_table` once PK is reshaped. Empty/future tables; out of #91/#92 scope.
3. **Cross-schema reads (module-boundary violations)** — OPD reads `registration.*` + `configurator.*` + `empi.*`, and configurator reads `master_global.modules`, via cross-schema raw SQL/ORM. Kept functionally as-is (rename made join columns consistent). Proper fix awaits the cross-module client/event decision (**D3/D8**, Phase 2/5). OPD's alembic 0002 backfill *requires* `registration.visit` to exist → `make db-migrate` ordering (registration before opd) is load-bearing.
4. **Untracked not-ours WIP** — `modules/master-data/alembic/versions/026_user_management_catalog_seed.py` duplicates the committed `026_um_catalog_seed` revision id (breaks in-place alembic for whoever has it); `infra/db/create-module-databases.sql` + `pg-init-trust.sh` are stale untracked. Left untouched (owner must reconcile/delete).
5. **Pre-existing `visitpad_units` test failures** — 3 tests in `test_visitpad_units_integration.py` return 422 (request-contract bug); fail on `dev` too, unrelated to this work.
6. **`hims-secrets.DATABASE_URL`** — the k8s deployments now reference `key: DATABASE_URL` for user-management/master-data/opd; ensure that key exists in the cluster secret store and the old `*_DATABASE_URL` keys are dropped (secrets live outside the repo).
