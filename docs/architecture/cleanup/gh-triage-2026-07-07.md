# GitHub Issue Triage — clean-house waves (W0–W5) on `dev--improved-v1`

> **Date:** 2026-07-07 · **Branch:** `dev--improved-v1` (off `dev@12963b72`) · **Scope:** the 148 commits
> `origin/dev..dev--improved-v1` (Phase 0/1 rebuild + deep-vet remediations + #51/#52 + waves W0–W5).
>
> **READ-ONLY / user fires the closes.** This is on-the-side work. The agent did **not** and must **not**
> mutate any issue, PR, or label. Every "CLOSEABLE" below is a *recommendation with evidence* — the user
> decides and clicks close. `gh` was authenticated and all 30 open issues were read.
>
> **Method:** `gh issue list --state open` (30 open) + per-issue `gh issue view` bodies, cross-referenced
> against the commit log, the cerbos policy tree, module dirs, and a live `nx run user-management:test`
> (used to confirm #138). "Realizing commit" cites the specific change; where a verdict depends on a check
> the user should run, it says so.

**Summary counts:** CLOSEABLE **11** · PARTIAL **6** · OBSOLETE/superseded **1** · UNTOUCHED (out of this
initiative's scope) **12** · Total open **30**.

---

## CLOSEABLE — the branch genuinely delivers the issue

| # | Title | Realizing commit / evidence | Note |
|---|---|---|---|
| 66 | Adopt drizzle-kit for TS migrations | `94753ed3` (empi reference) → `0f08202e` (7-module fan-out) → `41926d6f` (Python/DB tail); CI **drift gate** added in `4feeb64d` | All 8 TS modules on drizzle-kit + tracking table, verified against real Citus; hand-written `apply-migration.ts` pattern retired. Fully done + gated. |
| 91 | Rename `global_master`→`master_global`, `tenant_master`→`master_tenant` | `41926d6f` (constant swap + 373 literals across 41 migrations + guarded `ALTER SCHEMA` mig `044`); later absorbed into the W4 squash `c1f68ced` | Verified `master_global`/`master_tenant` on Citus, single alembic head. |
| 92 | Distribute Master Data catalog tables as Citus reference tables | `41926d6f` (#92 reference-table shape confirmed in `pg_dist_partition`) | `modules`/`permissions`/`system_roles` are reference tables. |
| 137 | `configurator:test` fails — `buildTarget` missing | `modules/configurator/project.json` now `"executor": "nx:run-commands"` (part of the green-the-gate work `9250bf33`/`69751e33`) | Matches every other module; verified the fix is in tree. |
| 138 | `user-management:test` — 23 `InvalidCapabilityKeyError` failures | `f92b200d` + `f84f2cfd` (canonicalize capability-key fixtures to Master Data L2 vocab) + `0c793421` + `9250bf33` | **Verified live this session:** `nx run user-management:test` = 66 files passed, 0 failures (was 23). |
| 89 | Cerbos: super-admin cross-tenant paths missing on `user.update`/`user.deactivate`/`role.revoke` | Paths now present: `infra/cerbos/policies/user_management/user.yaml` (update L138, deactivate L259) + `user_role_template.yaml` (revoke L27/L53) | Verify with `nx run infra-cerbos:compile` (`cerbos compile --tests`) before closing. |
| 11 | feat(empi): implement EMPI module | `modules/empi` built; drizzle-kit + Citus-distributed (`94753ed3`); #51 empi Cerbos PEP + tenant-leak fix (`502893fc`) | Module implemented end-to-end. |
| 33 | feat(integration-hub): implement Integration Platform | Absorbed from `origin/dev` PR #158 in the W1.5 merge `fa3b1e3e`; W2 identity hardening `2b206f8c` | Module exists (`modules/integration-hub`). See #159 for the residual post-merge review. |
| 34 | feat(record-foundation): implement Record Foundation | `modules/record-foundation` built; deep-vet remediation `8ce56210` (idempotent care-context, FK pre-check, real COUNT/paging + DB tests) | Module implemented + hardened. |
| 143 | [integration-hub] Phase 1a: restructure abdm-adapter + per-tenant credentials | Realized **upstream on `dev`** by PR #158; absorbed into this branch via W1.5 `fa3b1e3e` | Close if PR #158's Phase-1a scope is accepted as complete; pairs with #159's review checklist. |
| 15 | feat(configurator): implement Configurator module | `modules/configurator` built (Phase 1 + reach-in #1 `223d7818`) | Module implemented. **One follow-up remains open, not blocking closure:** the configurator Cerbos PEP (planned in `configurator-cerbos-pep-plan.md`, **not yet built**) — track separately. |

---

## PARTIAL — some of it landed; verify/track the remainder before closing

| # | Title | What landed (commit) | What remains |
|---|---|---|---|
| 159 | Post-merge review: integration-hub ABDM migration (PR #158) | W2 `2b206f8c` hardened integration-hub identity (added the missing identity gate) | The review checklist (re-read diff, verify Makefile/env/staging wiring) is not formally ticked. Keep open as the review tracker or close if #33/#143 satisfy it. |
| 125 | follow-up: unaddressed items from PR #122 (billing authz) | Cerbos tests now exist: `infra/cerbos/tests/billing_tariff_permissions_test.yaml`, `registration_visit_status_test.yaml`; billing hardening `d99724d0` | Verify the full requested set (`invoice_test`, `tariff_master_test`, `registration_test`, a delegated-caps equivalence test) and item 2 (registration resolver null-return pattern). |
| 113 | Nx project setup: health review findings | Phase 0 nx fixes `286eefe6` (broken `@nx/vite:test` in ts-sdk-sequence/ts-sdk-db → `vitest run`; ADR-0031 collision); SDK phantom test-targets fixed in the vet; drift gate added | Re-read the issue's full findings list against current state to confirm each is closed. |
| 36 | KB: How to write tests (unit + integration) | D1 convention now real + enforced: colocated tests relocated to `test/{unit,integration}` (`17d2a792` et al.); real-DB integration suites wired into CI one-DB-per-module (`8e0c9a59`, tagged #36) | The knowledge-base **doc** itself may still be owed; the mechanism/convention exists. |
| 20 | Master Data: build frontend CRUD pages | FE present under `services/web/src/features/master-data` (incl. medicine import path `bb72cf40`); web typecheck/lint gate greened (`0d74ef13`) | Confirm the intended set of CRUD pages is complete vs the issue's scope. |
| 22 | User Management: implementation roadmap | Large parts delivered: username-primary authn flip `e52f71f3`, authz #51/#48/#47, UM real-DB tests `f6aae0ad` | Roadmap/tracking issue — reconcile line-items; #60 (Phase 1.5) is the explicitly-deferred tail. |

---

## OBSOLETE / superseded — the initiative solved it a different (ratified) way

| # | Title | Verdict evidence | Note |
|---|---|---|---|
| 93 | Replace implicit super-admin role path in Cerbos with granular cross-tenant capabilities | **Superseded** by the bounded `scope:platform` operator model — `f9be418c` + **ADR-0035** (`docs/architecture/adr/0035-bounded-platform-operator-scope.md`). Platform authority is now a JWT `scope:platform` issued from a `platform_admins` table (`modules/user-management/migrations/0006_platform_admins.sql`), PDP-evaluated as an additive allow-rule; the god-mode all-caps seed (`sync-super-admin-capability-snapshots.ts`) was **deleted**. | The specific mechanism #93 proposed (per-action `cross-tenant-*` capability rows) was **not** adopted. Note: the legacy `roles:["super-admin"]` / `role_codes` paths still **coexist** in `user.yaml` alongside the new scope rules — if the user wants them physically removed, that's a small follow-up (see "Follow-ups"), not a reason to keep #93 open under its original framing. Recommend close-as-superseded, referencing ADR-0035. |

---

## UNTOUCHED — out of this initiative's scope (or explicitly deferred)

| # | Title | Verdict | Note |
|---|---|---|---|
| 30 | Event bridge: cross-module event pub/sub | UNTOUCHED | Explicitly deferred to **Phase 5** (D8 HTTP-first now; async bridge later). Reach-in #1 was done HTTP-first *without* the bus by design. |
| 60 | Phase 1.5 — UM authz live-JOIN + grant/deny overrides | UNTOUCHED | Future functional target; the W3 operator model is orthogonal (platform scope, not the per-user live-JOIN model). |
| 94 | Billing: replace `BILLING_ALLOW_DESK_OVERRIDES` env flag with `billing:bills:override-price` capability | UNTOUCHED | Not addressed on this branch. |
| 114 | [perf] Optimize frontend auth flow (5 RTT → 2) | UNTOUCHED | No perf-shaped change to the login RTT path on this branch. |
| 90 | Production reload UX: cookie-based bootstrap | UNTOUCHED | — |
| 112 | Remove dashboard mock layer once live `/dashboard/*` lands | UNTOUCHED | Gated on live endpoints that don't exist yet. |
| 100 | Refactor: collapse duplicate tenant-header logic in `api-client.ts` | UNTOUCHED | `services/web/src/lib/api-client.ts` not modified on this branch (BFF edge tenant-scope work `d47cfa6f` is backend, a different layer). |
| 95 | Visit registration: hydrate `consultation_fee` from `tariff_master` | UNTOUCHED | Enhancement, not in scope. |
| 128 | follow-up: unaddressed items from PR #127 | UNTOUCHED — **live bug remains** | Verified `invalidateModuleSlugMapCache()` in `services/user-management-svc/src/adapters/http-master-data-module-catalog-adapter.ts` still does **not** invalidate `moduleKindBySlugCache` (item 1). Stale `module_kind` persists until TTL after MD edits. Small real fix — see Follow-ups. |
| 25 | KB: Global reference data + tenant overrides | UNTOUCHED | Documentation issue; no KB doc authored this arc (the visitpad disjoint-global/tenant model that walked this back is in memory/LLDs, not a KB page). |
| 26 | KB: Why modules use separate PostgreSQL schemas | UNTOUCHED | Documentation issue; not written. |
| 169 | offline-approach-hims | UNTOUCHED | Proposal/discussion issue, outside the clean-house scope. |

---

## Follow-ups to OPEN (new issues the user may want to file)

These are consequences of the waves — deferred-with-a-gate items and residuals, not regressions:

1. **ADR-0035 operator-model follow-ups** (named in the ADR as the bounded-model's tail):
   - Operator-action **audit logging** (every `scope:platform` cross-tenant write recorded).
   - **Clinical-must-hit-PDP inventory test** — a guard proving clinical policies (opd/pharmacy/registration/empi/inventory/billing) stay **UNSCOPED** (the bound), so the operator scope can never satisfy a clinical action.
   - **`ts-sdk-tenant` header-first unify** — converge tenant-header derivation.
   - **ajv `removeAdditional`** — request-validation hardening.
2. **Configurator Cerbos PEP** — the last Phase-4 authz item; plan written + audited (`configurator-cerbos-pep-plan.md`), **not built**. (Relates to closing #15's residual.)
3. **integration-hub full resource Cerbos PEP** — W2 hardened *identity* only; resource-level PEP for hub is still owed.
4. **D5 master-data module/svc split** — deferred (risky restructure of a working service; pairs with the package-boundary call).
5. **OM17 pincode-family dedup** — shared-utility dedup analogous to the `@hims/ts-sdk-india` mobile-rules consolidation (`b88b2762`).
6. **historical-records phone variant** — reconcile the remaining phone-normalization variant against `@hims/ts-sdk-india`.
7. **Remove the legacy `roles:["super-admin"]` / `role_codes` paths from `user.yaml`** now that the bounded operator scope is authoritative (finishes what #93 asked for, under the ADR-0035 mechanism) — only if the user wants single-track policy.
8. **#128 cache-invalidation fix** — add `moduleKindBySlugCache.invalidate(...)` to `invalidateModuleSlugMapCache()` (can be folded into closing #128 rather than a new issue).

**Regressions:** none found. #128's `moduleKindBySlugCache` gap is **pre-existing** (from PR #127), not introduced by this branch.

---

## Note on the "abandoned authz stack #135–149"

Those are branches/PRs, not open issues — and in the **issue** space #135, #136, #139–142, #144–149 are
already closed. The only open issues in that numeric band are #143 (CLOSEABLE, above), #138 (CLOSEABLE),
and #137 (CLOSEABLE). No abandoned-stack issue needs a superseded-close here; the authz work landed
differently (per-module in-process PEPs #51 + bounded operator model ADR-0035).
