// Placeholder: timeline projection rebuild logic.
// Timeline index is rebuilt by replaying care_contexts + record_bundle_manifests
// + external_health_records. This module owns the rebuild function.
