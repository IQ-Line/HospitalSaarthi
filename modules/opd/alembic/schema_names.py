"""Schema constants for Alembic revisions (mirror of opd.core.schemas)."""

SCHEMA = "opd"
# Separate from master-data's public.alembic_version (same DB, independent revision chain).
VERSION_TABLE = "alembic_version_opd"
