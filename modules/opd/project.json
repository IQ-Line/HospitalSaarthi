{
  "name": "opd",
  "projectType": "library",
  "tags": ["type:module", "scope:opd", "language:python"],
  "namedInputs": {
    "default": ["{projectRoot}/**/*"],
    "production": [
      "default",
      "!{projectRoot}/tests/**/*",
      "!{projectRoot}/.env*",
      "!{projectRoot}/README.md"
    ]
  },
  "targets": {
    "setup": {
      "command": "uv sync",
      "options": {
        "cwd": "modules/opd"
      },
      "inputs": ["default"],
      "cache": true,
      "outputs": ["{projectRoot}/.venv"]
    },
    "lint": {
      "command": "uv run ruff check .",
      "options": {
        "cwd": "modules/opd"
      },
      "dependsOn": ["setup"],
      "inputs": ["default", "{projectRoot}/pyproject.toml"],
      "cache": true
    },
    "test": {
      "command": "uv run pytest",
      "options": {
        "cwd": "modules/opd"
      },
      "dependsOn": ["setup"],
      "inputs": ["default"],
      "cache": true
    },
    "migrate": {
      "command": "bash scripts/run-migrations.sh",
      "options": {
        "cwd": "modules/opd"
      },
      "dependsOn": ["setup"]
    },
    "db-migrate": {
      "command": "bash scripts/run-migrations.sh",
      "options": {
        "cwd": "modules/opd"
      },
      "dependsOn": ["setup"]
    },
    "seed-dev": {
      "command": "uv run python scripts/seed_dev_completed_visits.py",
      "options": {
        "cwd": "modules/opd"
      },
      "dependsOn": ["setup", "db-migrate"]
    }
  }
}
