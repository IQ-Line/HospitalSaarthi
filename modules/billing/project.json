{
  "name": "billing",
  "tags": ["type:module", "scope:billing", "language:typescript"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "options": {
        "outputPath": "dist/modules/billing",
        "main": "modules/billing/src/index.ts",
        "tsConfig": "modules/billing/tsconfig.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "modules/billing"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["modules/billing/src/**/*.ts"]
      }
    },
    "migrate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "psql \"${DATABASE_URL}\" -f modules/billing/migrations/0000_billing_master.sql && psql \"${DATABASE_URL}\" -f modules/billing/migrations/0001_bills_payments.sql"
      }
    }
  }
}
