# Cerbos tests: entitlement-filtered principal

# When principal hydration intersects stored grants with tenant entitlement,
# capabilities not in tenant_entitlement must not satisfy capability-based rules.

# Example scenario (documented — extend with concrete principals when seed data stable):
# - User has stored opd:visits:read but OPD module disabled for tenant
# - Principal effective capabilities exclude opd:visits:read
# - Cerbos DENY on opd visit resource requiring that capability

# See ADR-0032 and delegated_capabilities_test.yaml for principal attribute patterns.
