{
  "name": "ts-sdk-events",
  "tags": ["type:sdk", "scope:platform"],
  "targets": {
    "test": {
      "command": "vitest run",
      "options": {
        "cwd": "packages/ts-sdk-events"
      }
    },
    "lint": {
      "command": "eslint src/",
      "options": {
        "cwd": "packages/ts-sdk-events"
      }
    },
    "typecheck": {
      "command": "tsc -p tsconfig.json --noEmit",
      "options": {
        "cwd": "packages/ts-sdk-events"
      }
    }
  }
}
