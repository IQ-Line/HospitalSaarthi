{
  "extends": "./../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "ignoreDeprecations": "6.0",
    "types": ["node"],
    // See packages/ts-sdk-db/tsconfig.json — empty `paths` avoids `baseUrl` + `ignoreDeprecations`.
    "paths": {}
  },
  "include": ["src"],
  "exclude": ["dist", "node_modules", "**/*.test.ts"]
}
