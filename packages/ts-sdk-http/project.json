{
  "name": "ts-sdk-http",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/ts-sdk-http/src",
  "projectType": "library",
  "tags": ["scope:sdk", "type:sdk"],
  "targets": {
    "typecheck": {
      "command": "tsc -p tsconfig.json --noEmit",
      "options": {
        "cwd": "packages/ts-sdk-http"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
