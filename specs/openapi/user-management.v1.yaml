openapi: 3.1.0
info:
  title: HIMS User Management API
  version: 1.0.0
  description: >
    Tenant-scoped platform user profile, role-template, capability-grant, and principal-enrichment APIs.
    Credential issuance and session lifecycle are owned by better-auth (see implementation task 1A.7);
    they are not exposed as paths in this contract.
    Tenant scope authority is JWT-derived (`iq_tenant_id` claim). If `iq_tenant_id` header is
    provided for diagnostics, it must exactly match the JWT tenant claim.
servers:
  - url: /api/user-management
security:
  - bearerAuth: []
paths:
  /auth/me:
    get:
      tags:
        - Auth
      summary: Get current authenticated user
      description: >
        Returns the tenant-scoped platform user for the authenticated session (JWT `sub`
        resolved against the `users` table). This is profile data only — not the enriched
        authorization principal; use GET /auth/principal for that.
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
      responses:
        "200":
          description: Current user for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          description: Request validation error (including tenant header/JWT mismatch when header is provided).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: User not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
  /auth/principal:
    get:
      tags:
        - Auth
      summary: Get enriched principal for authorization
      description: >
        Returns the exact `PrincipalService.getPrincipal` payload attached by PEP as
        `request.cerbosPrincipal` (no recomputation). Semantically this matches the Cerbos principal
        used on checks: `id` is `principal.id`, `roles` is `principal.roles`, and `attributes` is
        `principal.attr` (same snake_case keys as `principalAttrsForCerbos` in `@hims/ts-sdk-authz`).
        The HTTP body nests attrs under `attributes` (Cerbos uses `attr`). Session-only JWT fields
        (`sessionId`, `iat`, `exp`, `iss`, …) are not included here — use the identity layer / token
        for those. If enrichment did not run, the service responds with 500 `CERBOS_PRINCIPAL_UNAVAILABLE`.
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
      responses:
        "200":
          description: Enriched principal (JWT claims plus resolved authorization data; token not included).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Principal"
        "400":
          description: Invalid request (e.g. principal resolution failed).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Platform user not found for this tenant (enrichment failed).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          description: >
            Enriched principal missing on the request (PEP mis-configuration) or other server fault.
            Code `CERBOS_PRINCIPAL_UNAVAILABLE` when principal enrichment did not run before this route.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
  /auth/permissions-map:
    get:
      tags:
        - Auth
      summary: Cerbos-backed SPA permission map (UX only)
      description: >
        Returns a nested map (module → feature → action → boolean) derived from the same Cerbos PDP
        checks as User Management APIs. Intended for frontend navigation and control visibility only;
        every protected API must still enforce authorization server-side.
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
      responses:
        "200":
          description: Permission map for the authenticated principal in the active tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PermissionsUxMapResponse"
        "400":
          description: Request validation error (including tenant header/JWT mismatch when header is provided).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"
  /capabilities:
    get:
      tags:
        - Capabilities
      summary: List canonical capability catalog
      description: >
        Returns the canonical capability catalog. Capabilities are the atomic machine-readable
        authorization primitives used by Cerbos and composed into tenant-scoped roles.
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
      responses:
        "200":
          description: Global capability catalog used for role composition.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Capability"
        "400":
          description: Request validation error (including tenant header/JWT mismatch when header is provided).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"
  /capabilities/{id}:
    get:
      tags:
        - Capabilities
      summary: Get a canonical capability by id
      description: >
        Returns a single capability from the global capability catalog. Capabilities remain the
        canonical authorization primitives and are not tenant-scoped.
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - $ref: "#/components/parameters/CapabilityIdPath"
      responses:
        "200":
          description: Capability found in the canonical catalog.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Capability"
        "400":
          description: Request validation error (including malformed capability id).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Capability not found in the catalog.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
  /roles:
    get:
      tags:
        - Roles
      summary: List tenant-scoped roles
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
      responses:
        "200":
          description: Roles defined for the active tenant.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Role"
        "400":
          description: Request validation error (including tenant header/JWT mismatch when header is provided).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags:
        - Roles
      summary: Create a tenant-scoped role
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - code
                - display_name
              properties:
                code:
                  type: string
                  minLength: 1
                  description: Canonical tenant-scoped role code.
                display_name:
                  type: string
                  minLength: 1
                  description: Human-readable role label.
                description:
                  type: string
                  nullable: true
                is_system:
                  type: boolean
                  description: System-managed role flag.
                status:
                  type: string
                  enum: [active, inactive]
      responses:
        "201":
          description: Role created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Role"
        "400":
          description: Invalid input.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Duplicate role code in this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
  /roles/{id}:
    get:
      tags:
        - Roles
      summary: Get a tenant-scoped role by id
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - $ref: "#/components/parameters/RoleIdPath"
      responses:
        "200":
          description: Role found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Role"
        "400":
          description: Request validation error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Role not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
    patch:
      tags:
        - Roles
      summary: Update a tenant-scoped role
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - $ref: "#/components/parameters/RoleIdPath"
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                code:
                  type: string
                  minLength: 1
                display_name:
                  type: string
                  minLength: 1
                description:
                  type: string
                  nullable: true
                is_system:
                  type: boolean
                status:
                  type: string
                  enum: [active, inactive]
      responses:
        "200":
          description: Role updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Role"
        "400":
          description: Invalid input.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Role not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "409":
          description: Duplicate role code in this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags:
        - Roles
      summary: Delete a tenant-scoped role
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - $ref: "#/components/parameters/RoleIdPath"
      responses:
        "200":
          description: Deleted role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Role"
        "400":
          description: Request validation error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Role not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "409":
          description: Role template still has active user associations and cannot be deleted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
  /roles/{id}/capabilities:
    get:
      tags:
        - Roles
      summary: List capabilities composed into a role
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - $ref: "#/components/parameters/RoleIdPath"
      responses:
        "200":
          description: Capabilities currently composed into the role.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Capability"
        "400":
          description: Request validation error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Role not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
    put:
      tags:
        - Roles
      summary: Replace the capabilities composed into a role
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - $ref: "#/components/parameters/RoleIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - capability_ids
              properties:
                capability_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
                  description: Canonical capability ids to compose into the role.
      responses:
        "200":
          description: Updated role composition.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Capability"
        "400":
          description: Invalid input.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Role or capability not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
  /users:
    get:
      tags:
        - Users
      summary: List tenant-scoped platform users (ABAC-filtered)
      description: >
        Returns users for the active tenant. Route access is gated on `user.list` (PEP); the list
        body is filtered using a single Cerbos PlanResources call for `user.read` and the returned
        query-plan AST (evaluated locally against each row), not per-row CheckResources.
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
      responses:
        "200":
          description: Users visible to the caller under Cerbos ABAC rules for this tenant.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/User"
        "400":
          description: Request validation error (including tenant header/JWT mismatch when header is provided).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags:
        - Users
      summary: Create a tenant-scoped user with login access
      description: >
        Creates the tenant-scoped platform `users` row for the current tenant (`iq_tenant_id`
        is the Citus distribution key and is taken from request context, not the request body),
        provisions the linked better-auth login account for the same principal, and optionally
        writes direct user capabilities from `capability_ids` plus copied template access from
        `role_template_ids`. When `role_template_capability_ids` is present, exactly one role id must
        be listed in `role_template_ids`, and only those capabilities (each belonging to that role)
        are granted with source `role_template`; omit the field to copy the full role composition.
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - full_name
                - email
                - password
              properties:
                full_name:
                  type: string
                  minLength: 1
                  description: Display name for the principal (required platform profile field).
                email:
                  type: string
                  format: email
                  description: Login email used to provision the current minimal better-auth account and retained as business contact email.
                password:
                  type: string
                  minLength: 8
                  description: Initial login password for the linked better-auth account.
                phone:
                  type: string
                  nullable: true
                  description: Business contact phone.
                username:
                  type: string
                  nullable: true
                  description: Optional login handle; unique per tenant when set.
                org_id:
                  type: string
                  format: uuid
                  nullable: true
                  description: Organization scope (configurator organizations.id); logical reference only.
                department:
                  type: string
                  nullable: true
                  description: Department for ABAC enrichment when stored on the profile.
                clearance_tier_required:
                  type: integer
                  minimum: 0
                  maximum: 3
                  description: Initial minimum clearance tier required to access this user record (ABAC).
                capability_ids:
                  type: array
                  description: Direct user capability ids to persist immediately after the user is created.
                  items:
                    type: string
                    format: uuid
                role_template_ids:
                  type: array
                  description: Optional role-template ids to apply immediately after the user is created.
                  items:
                    type: string
                    format: uuid
                role_template_capability_ids:
                  type: array
                  description: >
                    Optional subset of capability ids to grant from the role template(s). When this
                    array is present, `role_template_ids` must contain exactly one id, and every
                    entry must be a capability currently assigned to that role. Omit to grant the
                    full capability set defined on each role template.
                  items:
                    type: string
                    format: uuid
      responses:
        "201":
          description: Platform user and linked login account created for the tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          description: Invalid input (e.g. malformed body or invalid field values).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"
  /users/{id}:
    get:
      tags:
        - Users
      summary: Get a tenant-scoped user by id
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Platform user id within the current tenant.
      responses:
        "200":
          description: User found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          description: Request validation error (including tenant header/JWT mismatch when header is provided).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: User not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
    patch:
      tags:
        - Users
      summary: Update a tenant-scoped user
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Platform user id within the current tenant.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                full_name:
                  type: string
                  minLength: 1
                  description: Display name for the principal.
                email:
                  type: string
                  nullable: true
                  description: Business contact email (nullable, non-unique per tenant); not the better-auth synthetic anchor.
                phone:
                  type: string
                  nullable: true
                  description: Business contact phone.
                username:
                  type: string
                  nullable: true
                  description: Optional login handle; unique per tenant when set.
                org_id:
                  type: string
                  format: uuid
                  nullable: true
                  description: Organization scope (configurator organizations.id); logical reference only.
                department:
                  type: string
                  nullable: true
                  description: Department for ABAC when updated on the profile.
                clearance_tier_required:
                  type: integer
                  minimum: 0
                  maximum: 3
                  description: Minimum clearance tier for sensitive access (ABAC).
                status:
                  type: string
                  enum: [active, inactive, suspended]
                  description: Platform user lifecycle state.
                auth_user_id:
                  type: string
                  format: uuid
                  nullable: true
                  description: External identity anchor (e.g. better-auth user id) when linked.
      responses:
        "200":
          description: User updated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          description: Invalid input (e.g. malformed body or invalid field values).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: User not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
  /users/{id}/roles:
    get:
      tags:
        - RoleTemplates
      summary: List applied role templates for a tenant-scoped user
      description: >
        Returns the role templates applied to the specified user. These rows are reporting and admin
        convenience only; runtime authorization still resolves from persisted `user_capabilities`.
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: Role templates currently associated with the user within the active tenant.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AppliedRoleTemplate"
        "400":
          description: Request validation error (including malformed user id).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: User not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags:
        - RoleTemplates
      summary: Apply a role template to a user
      description: >
        Creates a `user_roles` association and copies the template's current capabilities into
        `user_capabilities` during the same write flow. Later edits to the role template do not
        retroactively change the user's access.
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - $ref: "#/components/parameters/UserIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - role_id
              properties:
                role_id:
                  type: string
                  format: uuid
                  description: Role-template id within the current tenant.
      responses:
        "201":
          description: Role template applied and capability copy completed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppliedRoleTemplate"
        "400":
          description: Malformed request body or invalid UUID fields.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: User or role template not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "409":
          description: This role template is already applied to the user.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
  /users/{id}/roles/{roleId}:
    delete:
      tags:
        - RoleTemplates
      summary: Remove a role-template association from a user
      description: >
        Removes the `user_roles` association only. Previously copied `user_capabilities` remain
        untouched until an explicit capability edit changes them.
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - $ref: "#/components/parameters/UserIdPath"
        - name: roleId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Role-template id within the current tenant.
      responses:
        "200":
          description: Role-template association removed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AppliedRoleTemplate"
        "400":
          description: Invalid or missing path parameters.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: User, role template, or user-role association not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
  /users/{id}/capabilities:
    get:
      tags:
        - Capabilities
      summary: Get persisted user capability grants
      description: >
        Returns the user's persisted base grants split into direct grants, copied template grants,
        and applied role-template associations. Delegated overlays are excluded from this endpoint.
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: Persisted user capability grants plus applied role-template associations.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserCapabilitiesSnapshot"
        "400":
          description: Request validation error (including malformed user id).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: User not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
    put:
      tags:
        - Capabilities
      summary: Replace direct user capability grants
      description: >
        Replaces the user's direct manual capability grants. Copied template grants remain
        untouched until an explicit capability edit changes them.
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - $ref: "#/components/parameters/UserIdPath"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - capability_ids
              properties:
                capability_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
      responses:
        "200":
          description: Direct user grants replaced; response returns the refreshed persisted grant snapshot.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserCapabilitiesSnapshot"
        "400":
          description: Invalid input (e.g. malformed body or invalid UUID format).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: User or capability not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
  /users/{id}/effective-capabilities:
    get:
      tags:
        - Capabilities
      summary: Get effective runtime capabilities for a user
      description: >
        Returns the runtime-effective capability keys resolved from persisted user grants plus
        delegated overlays and clearance context. Role traversal is not part of this read path.
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: Effective runtime authorization snapshot for the specified user.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserEffectiveCapabilities"
        "400":
          description: Request validation error (including malformed user id).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: User not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
  /users/{id}/deactivate:
    post:
      tags:
        - Users
      summary: Deactivate a tenant-scoped platform user
      description: >
        Soft-deactivates the user by setting `status` to `inactive`. Idempotent when already inactive.
        Uses Cerbos action `user.deactivate` (not `user.update`) so policies can separate profile edits from deactivation.
      parameters:
        - $ref: "#/components/parameters/IqTenantIdHeader"
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Platform user id within the current tenant.
      responses:
        "200":
          description: User is now inactive (or was already inactive).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          description: Request validation error (including tenant header/JWT mismatch when header is provided).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: User not found for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "500":
          $ref: "#/components/responses/InternalError"
components:
  responses:
    Forbidden:
      description: Cerbos PDP denied the action for this principal (PEP).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorMessage"
    InternalError:
      description: Unexpected server failure while processing the request.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorMessage"
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Platform JWT; runtime rejects missing/malformed bearer tokens and invalid/expired tokens.
  parameters:
    IqTenantIdHeader:
      name: iq_tenant_id
      in: header
      required: false
      deprecated: true
      description: >
        Optional diagnostic header. Canonical tenant scope is JWT `iq_tenant_id`.
        If this header is supplied, runtime requires exact match with JWT tenant scope.
      schema:
        type: string
        format: uuid
    CapabilityIdPath:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: Canonical capability id from the global capability catalog.
    RoleIdPath:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: Tenant-scoped role id.
    UserIdPath:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
      description: Platform user id within the current tenant.
  schemas:
    Capability:
      type: object
      required:
        - id
        - capability_key
        - module
        - feature
        - action
        - display_name
        - is_active
      properties:
        id:
          type: string
          format: uuid
        capability_key:
          type: string
          description: Canonical machine-readable capability key.
        module:
          type: string
        feature:
          type: string
        action:
          type: string
        display_name:
          type: string
        description:
          type: string
          nullable: true
        is_active:
          type: boolean
    Role:
      type: object
      required:
        - id
        - code
        - display_name
        - is_system
        - status
      properties:
        id:
          type: string
          format: uuid
        code:
          type: string
        display_name:
          type: string
        description:
          type: string
          nullable: true
        is_system:
          type: boolean
        status:
          type: string
          enum: [active, inactive]
    AppliedRoleTemplate:
      type: object
      required:
        - id
        - user_id
        - role_id
        - assigned_by_user_id
        - assigned_at
        - role
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        role_id:
          type: string
          format: uuid
        assigned_by_user_id:
          type: string
          format: uuid
          nullable: true
        assigned_at:
          type: string
          format: date-time
        role:
          $ref: "#/components/schemas/Role"
    UserCapabilityGrant:
      type: object
      required:
        - id
        - user_id
        - capability_id
        - capability_key
        - module
        - feature
        - action
        - display_name
        - grant_source
        - source_role_id
        - granted_by_user_id
        - granted_at
        - revoked_at
        - revoked_by_user_id
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        capability_id:
          type: string
          format: uuid
        capability_key:
          type: string
        module:
          type: string
        feature:
          type: string
        action:
          type: string
        display_name:
          type: string
        description:
          type: string
          nullable: true
        grant_source:
          type: string
          enum: [manual, role_template, delegated, system]
        source_role_id:
          type: string
          format: uuid
          nullable: true
        granted_by_user_id:
          type: string
          format: uuid
          nullable: true
        granted_at:
          type: string
          format: date-time
        revoked_at:
          type: string
          format: date-time
          nullable: true
        revoked_by_user_id:
          type: string
          format: uuid
          nullable: true
    UserCapabilitiesSnapshot:
      type: object
      required:
        - direct_grants
        - copied_grants
        - role_templates
      properties:
        direct_grants:
          type: array
          items:
            $ref: "#/components/schemas/UserCapabilityGrant"
        copied_grants:
          type: array
          items:
            $ref: "#/components/schemas/UserCapabilityGrant"
        role_templates:
          type: array
          items:
            $ref: "#/components/schemas/AppliedRoleTemplate"
    UserEffectiveCapabilities:
      type: object
      required:
        - capability_keys
        - delegated_capability_keys
        - clearances
        - um_clearance_effective_tier
      properties:
        capability_keys:
          type: array
          items:
            type: string
        delegated_capability_keys:
          type: array
          items:
            type: string
        clearances:
          type: object
          additionalProperties:
            type: string
        um_clearance_effective_tier:
          type: integer
          minimum: 0
          maximum: 3
    PermissionsUxMap:
      type: object
      description: >
        Nested map keyed by module id (e.g. user-management), then feature id (e.g. users), then action id (e.g. read).
        Values are Cerbos-backed UX hints from GET /auth/permissions-map.
      additionalProperties:
        type: object
        additionalProperties:
          type: object
          additionalProperties:
            type: boolean
    PermissionsUxMapResponse:
      type: object
      required:
        - map
      properties:
        map:
          $ref: "#/components/schemas/PermissionsUxMap"
    ErrorMessage:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          description: Machine-readable error code.
        message:
          type: string
        correlation_id:
          type: string
          description: Request correlation identifier.
    User:
      type: object
      required:
        - id
        - full_name
        - status
      properties:
        id:
          type: string
          format: uuid
        full_name:
          type: string
        email:
          type: string
          nullable: true
          description: Business contact email when stored.
        phone:
          type: string
          nullable: true
          description: Business contact phone when stored.
        auth_user_id:
          type: string
          format: uuid
          nullable: true
          description: External identity anchor when linked.
        username:
          type: string
          nullable: true
          description: Login handle when set.
        org_id:
          type: string
          format: uuid
          nullable: true
          description: Organization scope when set.
        department:
          type: string
          nullable: true
          description: Department attribute for ABAC (persisted profile field).
        clearance_tier_required:
          type: integer
          minimum: 0
          maximum: 3
          description: >
            Minimum principal clearance tier (from Cerbos `um_clearance_effective_tier`) required for
            user.read / user.update / user.deactivate on this record. 0 = standard.
        status:
          type: string
          enum: [active, inactive, suspended]
          description: Platform user lifecycle state.
    Principal:
      type: object
      description: >
        Authorization snapshot for PDP alignment. Maps to Cerbos principal as
        `{ id, roles, attr: attributes }` after `request.user` is synced from this payload in the
        enrichment hook (see `@hims/user-management` principal enrichment plugin).
      required:
        - id
        - roles
        - attributes
      properties:
        id:
          type: string
          description: Same value as Cerbos `principal.id` and JWT `sub` (platform user id).
        roles:
          type: array
          items:
            type: string
        attributes:
          type: object
          required:
            - iq_tenant_id
            - department
            - org_id
            - capabilities
            - delegated_capabilities
            - clearances
            - um_clearance_effective_tier
          properties:
            iq_tenant_id:
              type: string
            department:
              type: string
              nullable: true
            org_id:
              type: string
              nullable: true
            capabilities:
              type: array
              items:
                type: string
            delegated_capabilities:
              type: array
              items:
                type: string
            clearances:
              type: object
              additionalProperties:
                type: string
              description: Map of clearance types to levels (e.g. psychiatric, vip).
            um_clearance_effective_tier:
              type: integer
              minimum: 0
              maximum: 3
              description: >
                Highest clearance tier derived from `clearances` for comparison with
                `resource.attr.required_clearance` on user resources in Cerbos.
    RoleAssignment:
      type: object
      required:
        - id
        - user_id
        - role_id
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        role_id:
          type: string
          format: uuid
