openapi: 3.1.0
info:
  title: Record Foundation API
  version: 1.0.0
  description: >
    Record Foundation API — care-context registry and immutable FHIR Document Bundle vault.
    Simplified CRUD service with 2 tables. Does NOT own clinical source data (operational
    modules), patient identity (EMPI), or ABDM artifacts (Integration Hub).
    Care contexts are immutable after creation — create a new one to supersede an existing context.
servers:
  - url: http://localhost:3004/api/record-foundation/v1
    description: Local development
security:
  - bearerAuth: []
tags:
  - name: Care Contexts
    description: Discoverable health-record units.
  - name: Bundles
    description: FHIR Document Bundle storage.
paths:
  /care-contexts:
    get:
      operationId: listCareContexts
      summary: List care contexts for a patient
      tags: [Care Contexts]
      parameters:
        - { name: patient_id, in: query, required: false, schema: { type: string, format: uuid } }
        - { name: status, in: query, required: false, schema: { $ref: "#/components/schemas/CareContextStatus" } }
        - { name: limit, in: query, required: false, description: "When set, caps the page size (max 200) and pages via offset. Omit to return ALL matching rows.", schema: { type: integer, minimum: 1, maximum: 200 } }
        - { name: offset, in: query, required: false, schema: { type: integer, minimum: 0 } }
      responses:
        "200":
          description: Care context list.
          content:
            application/json:
              schema:
                type: object
                required: [data, total]
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/CareContext" }
                  total: { type: integer, minimum: 0 }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createCareContext
      summary: Create a care context (idempotent on the source tuple)
      description: >-
        Idempotent on (source_origin, source_system_id, source_record_type,
        source_record_id). A fresh record returns 201; redelivery of the SAME
        source tuple returns 200 with the existing care context (clinical
        contexts arrive over an at-least-once path). A NULL source_record_id
        never deduplicates — each such request creates a new context.
      tags: [Care Contexts]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateCareContextRequest" }
      responses:
        "201":
          description: Care context created.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data: { $ref: "#/components/schemas/CareContext" }
        "200":
          description: Existing care context returned (idempotent replay of the same source tuple).
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data: { $ref: "#/components/schemas/CareContext" }
        "400": { $ref: "#/components/responses/BadRequest" }
  /care-contexts/{id}:
    get:
      operationId: getCareContext
      summary: Fetch one care context
      tags: [Care Contexts]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Care context found.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data: { $ref: "#/components/schemas/CareContext" }
        "404": { $ref: "#/components/responses/NotFound" }
  /care-contexts/{careContextId}/bundles:
    get:
      operationId: listCareContextBundles
      summary: List bundles for a care context
      tags: [Bundles]
      parameters:
        - { name: careContextId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Bundle list.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Bundle" }
        "404": { $ref: "#/components/responses/NotFound" }
  /bundles/{id}:
    get:
      operationId: getBundle
      summary: Fetch a FHIR Document Bundle by id
      tags: [Bundles]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Bundle found.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data: { $ref: "#/components/schemas/Bundle" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: storeBundle
      summary: Store a FHIR Document Bundle against an existing care context
      tags: [Bundles]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StoreBundleRequest" }
      responses:
        "201":
          description: Bundle stored.
          content:
            application/json:
              schema:
                type: object
                required: [data]
                properties:
                  data: { $ref: "#/components/schemas/Bundle" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "404":
          description: Referenced care context does not exist.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/ProblemDetails" }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  responses:
    BadRequest:
      description: Invalid request.
      content:
        application/problem+json:
          schema: { $ref: "#/components/schemas/ProblemDetails" }
    Unauthorized:
      description: Missing or invalid bearer token.
      content:
        application/problem+json:
          schema: { $ref: "#/components/schemas/ProblemDetails" }
    Forbidden:
      description: Principal is not authorized.
      content:
        application/problem+json:
          schema: { $ref: "#/components/schemas/ProblemDetails" }
    NotFound:
      description: Resource not found.
      content:
        application/problem+json:
          schema: { $ref: "#/components/schemas/ProblemDetails" }
  schemas:
    ProblemDetails:
      type: object
      description: RFC 7807 problem+json error envelope.
      required: [type, title, status]
      properties:
        type: { type: string, format: uri }
        title: { type: string }
        status: { type: integer }
        detail: { type: string }
        instance: { type: string }
    CareContextStatus:
      type: string
      enum: [active, inactive, archived]
    SourceOrigin:
      type: string
      enum: [platform_module, legacy_system, external_abdm]
    SourceRecordType:
      type: string
      description: Clinical record type identifier. Values are defined per operational module (e.g. opd_visit, lab_report, discharge_summary).
    CareContext:
      type: object
      required:
        - id
        - patient_id
        - source_origin
        - source_system_id
        - source_record_type
        - display
        - period_start
        - status
        - created_at
      properties:
        id: { type: string, format: uuid }
        iq_tenant_id: { type: string, format: uuid }
        patient_id: { type: string, format: uuid }
        source_origin: { $ref: "#/components/schemas/SourceOrigin" }
        source_system_id: { type: string }
        source_record_type: { $ref: "#/components/schemas/SourceRecordType" }
        source_record_id: { type: string, nullable: true }
        encounter_id: { type: string, format: uuid, nullable: true }
        display: { type: string }
        period_start: { type: string, format: date-time }
        period_end: { type: string, format: date-time, nullable: true }
        status: { $ref: "#/components/schemas/CareContextStatus" }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        created_by: { type: string, format: uuid, nullable: true }
        updated_by: { type: string, format: uuid, nullable: true }
    CreateCareContextRequest:
      type: object
      required:
        - patient_id
        - source_origin
        - source_system_id
        - source_record_type
        - display
        - period_start
      additionalProperties: false
      properties:
        patient_id: { type: string, format: uuid }
        source_origin: { $ref: "#/components/schemas/SourceOrigin" }
        source_system_id: { type: string }
        source_record_type: { $ref: "#/components/schemas/SourceRecordType" }
        source_record_id: { type: string }
        encounter_id: { type: string, format: uuid }
        display: { type: string }
        period_start: { type: string, format: date-time }
        period_end: { type: string, format: date-time }
        status: { $ref: "#/components/schemas/CareContextStatus" }
    Bundle:
      type: object
      required:
        - id
        - care_context_id
        - bundle_kind
        - fhir_profile_url
        - fhir_profile_version
        - bundle_json
        - produced_at
      properties:
        id: { type: string, format: uuid }
        iq_tenant_id: { type: string, format: uuid }
        care_context_id: { type: string, format: uuid }
        bundle_kind: { type: string }
        fhir_profile_url: { type: string, format: uri }
        fhir_profile_version: { type: string }
        producer_kind: { type: string }
        producer_id: { type: string }
        bundle_json: { type: object, description: Full FHIR R4 Bundle JSON }
        bundle_size_bytes: { type: integer }
        produced_at: { type: string, format: date-time }
        stored_at: { type: string, format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        created_by: { type: string, format: uuid, nullable: true }
        updated_by: { type: string, format: uuid, nullable: true }
    StoreBundleRequest:
      type: object
      required:
        - care_context_id
        - bundle_kind
        - fhir_profile_url
        - fhir_profile_version
        - bundle_json
        - produced_at
      additionalProperties: false
      properties:
        care_context_id: { type: string, format: uuid }
        bundle_kind: { type: string }
        fhir_profile_url: { type: string, format: uri }
        fhir_profile_version: { type: string }
        producer_kind: { type: string, default: "platform_module" }
        producer_id: { type: string, description: "Defaults to the current tenant ID (x-tenant-id header) if omitted." }
        bundle_json: { type: object, description: Full FHIR R4 Bundle JSON }
        produced_at: { type: string, format: date-time }
