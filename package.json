{
  "name": "hims-platform",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "nx run-many -t serve --parallel=10",
    "dev:web-stack": "nx run-many -t serve -p web,bff,master-data --parallel=5",
    "lint": "nx affected -t lint",
    "test": "nx affected -t test",
    "build": "nx run-many -t build",
    "ci:pr": "nx affected -t lint typecheck test"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["nx", "esbuild"]
  },
  "devDependencies": {
    "@nx/eslint": "^22.7.1",
    "@nx/js": "^22.7.1",
    "eslint": "^10.3.0",
    "nx": "^22.7.1",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3"
  }
}
