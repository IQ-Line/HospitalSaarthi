{
  "name": "analytics-svc",
  "projectType": "application",
  "tags": ["type:service", "scope:analytics", "language:typescript", "deploy:aks"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsup --config tsup.config.ts",
        "cwd": "services/analytics-svc"
      },
      "outputs": ["{projectRoot}/dist"],
      "cache": true
    },
    "serve": {
      "command": "tsx watch src/main.ts",
      "options": {
        "cwd": "services/analytics-svc"
      }
    }
  }
}
