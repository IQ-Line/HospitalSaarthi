# =============================================================================
# Configurator Service — tenants, organizations, module enablement
# =============================================================================
# Loaded after the workspace-root `.env`. DATABASE_URL, JWKS_URL, CERBOS_URL
# inherit from root.
# =============================================================================

CONFIGURATOR_SVC_PORT=3101

# Internal S2S routes (GET /integration-profiles/by-hip/:hipId). Unset in local dev = no header required.
# Generate a long random string (one per environment), e.g.:
# openssl rand -hex 32
# CONFIGURATOR_INTERNAL_API_KEY=change-me-in-production
