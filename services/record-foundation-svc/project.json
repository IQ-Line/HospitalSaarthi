{
  "name": "record-foundation-svc",
  "projectType": "application",
  "tags": ["type:service", "scope:record-foundation", "language:typescript", "deploy:aks"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsup --config tsup.config.ts",
        "cwd": "services/record-foundation-svc"
      },
      "outputs": ["{projectRoot}/dist"],
      "cache": true
    },
    "serve": {
      "command": "tsx watch src/main.ts",
      "options": {
        "cwd": "services/record-foundation-svc"
      }
    }
  }
}
