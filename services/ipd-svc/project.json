{
  "name": "ipd-svc",
  "projectType": "application",
  "tags": ["type:service", "scope:ipd", "language:typescript", "deploy:aks"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsup --config tsup.config.ts",
        "cwd": "services/ipd-svc"
      },
      "outputs": ["{projectRoot}/dist"],
      "cache": true
    },
    "serve": {
      "command": "tsx watch --include '../../modules/ipd/src' src/main.ts",
      "options": {
        "cwd": "services/ipd-svc",
        "envFile": "{workspaceRoot}/.env"
      }
    }
  }
}
