# =============================================================================
# Pharmacy Service — custom counter dispense + manual billing
# =============================================================================

PHARMACY_SVC_PORT=3004

# Upstream (inherits OPD_URL / MASTER_DATA_URL / PHARMACY_SVC_PORT from root .env when unset)
# OPD_URL=http://localhost:8020
# MASTER_DATA_URL=http://localhost:8010

# PHARMACY_SKIP_MIGRATE=false

# Shared secret for service-to-service internal routes (OPD queue projection push).
# PHARMACY_INTERNAL_API_KEY=change-me-in-production
