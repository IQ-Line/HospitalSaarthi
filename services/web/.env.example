# =============================================================================
# Web — React + Vite frontend
# =============================================================================
# Vite has its own env-file flow: only `VITE_*` keys are exposed to the
# browser bundle (see https://vitejs.dev/guide/env-and-mode). Non-VITE keys
# in this file are ignored by Vite at build time.
# =============================================================================

# Points at the BFF (NOT the Python/Node service ports) so the browser gets
# CORS + auth headers. Do not point at :8010 directly.
VITE_API_BASE_URL=http://localhost:3100

# Cerbos browser client (HTTP admin API on :3592; Node PDP gRPC is :3593).
VITE_CERBOS_URL=http://localhost:3592

# --- Optional ----------------------------------------------------------------
# Default tenant UUID for dev login flow.
# VITE_DEFAULT_IQ_TENANT_ID=

# Optional: call registration-svc directly (bypass Vite proxy). Default uses same-origin
# `/api/registration/v1` → Vite proxies to http://localhost:3006 (requires registration-svc).
# VITE_REGISTRATION_SERVICE_ORIGIN=http://localhost:3006

# Talk directly to abdm-adapter-svc (bypass Vite proxy). Dev uses proxy when unset.
# VITE_ABDM_ADAPTER_ORIGIN=http://localhost:3007

# --- LC/NC (page + form builder runtime) -----------------------------------
# Loaded from workspace-root `.env` (see vite.config `envDir`).
# When ON, authenticated routes resolve against page-builder; unmapped routes use legacy UI.
# VITE_LC_NC=OFF
# VITE_CLIENT_ID=
# VITE_FORM_WORKFLOW_BUILDER_URL=http://localhost:5000/workflow-backend
# Or same-origin via Vite proxy: VITE_FORM_WORKFLOW_BUILDER_URL=/workflow-backend
# VITE_BUSINESS_API_BASE=http://localhost:3100
