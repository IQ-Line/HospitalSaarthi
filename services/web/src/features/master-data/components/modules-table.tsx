/**
 * TODO: Build modules list using @pulse/ui Table component + useModules() hook.
 * See packages/pulse-ui/src/table.tsx for the Table primitive.
 * Reference IQSandbox for UX patterns (screen layout, filters, actions).
 */
export function ModulesTable() {
  return <div className="p-6 text-gray-400">Modules table — implement with @pulse/ui Table</div>;
}
