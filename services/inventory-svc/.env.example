# Loaded after the workspace-root `.env`. DATABASE_URL is required.
INVENTORY_SVC_PORT=3008
# Set true in k8s when migrations run via CI/Makefile instead of on pod boot.
# INVENTORY_SKIP_MIGRATE=true
